# 24 — C4.5a Evidence Safety EV0
## Post-Repair Deterministic Reproduction

**Project:** Continuity-Aware Distributed Inference  
**Milestone:** C4.5a under C4.5 / S4 Evidence Safety  
**Tracking:** #70 under #69 / umbrella #44  
**Repaired main base:** `77411cb612c85db8f0171e315a762aa0f2044db4`  
**Preserved pre-repair measurement:** PR #71 at `855c918dad2aff9d6235a436eba974b97d05fee3`  
**Repair gate:** #72 / PR #73  
**Status:** POST-REPAIR VALIDATION CANDIDATE

---

## 1. Scientific sequence

C4.5a first measured the original closed C1 stack before any repair. The mandatory mixed valid+ambiguous Evidence case produced:

```text
reconcile(finalize, set) = AMBIGUOUS
finalize_request(...)     = APPLIED
```

Across every policy sharing C1 authority, that pre-repair stack measured:

```text
ACR  = 1/9
SSER = 1/9
O4   = 1 faulted operation
```

That result is preserved in closed, unmerged PR #71. It is not rewritten by this post-repair reproduction.

Repair gate #72 then explicitly reopened C1. PR #73 added a central fail-closed rule for supplied `EvidenceStatus.AMBIGUOUS` Evidence on the three current Paper-1 correctness-sensitive actions:

```text
finalize
consume_state
commit_migration
```

PR #73 exact head `15a2ef451c89a5d530e37dc72f62a501251a7ddd` passed the full Python 3.11/3.12/3.13 matrix and bounded exact-head review, then squash-merged as:

`77411cb612c85db8f0171e315a762aa0f2044db4`

This document records the required reproduction from that repaired mainline.

---

## 2. Hypothesis slice

C4.5a evaluates the deterministic EV0 component of H3/S4:

> correctness-sensitive actions that require explicit sufficient Evidence and fail closed on ambiguity prevent silent semantic commits under covered Evidence faults.

C4.5a alone does not adjudicate H3/S4. Adversarial EV0 (C4.5b) and measured EV1 (C4.5c) remain required.

---

## 3. Mandatory deterministic corpus

The post-repair reproduction reuses the same ten-scenario corpus and independent oracle as the preserved pre-repair measurement:

```text
S4-AMBIGUOUS-ONLY
S4-VALID-PLUS-AMBIGUOUS-SAME-SCOPE
S4-STALE-ONLY
S4-UNKNOWN-ONLY
S4-FAILED-ONLY
S4-ESTIMATED-ONLY
S4-DERIVED-ONLY
S4-WRONG-SCOPE
S4-EXPIRED-VALID-UNTIL
S4-VALID-EXACT-CONTROL
```

Nine scenarios are faulted and carry one fixed exogenous paired ACR EventID each. The valid exact control is non-faulted and contributes no ACR denominator event.

Ground truth remains policy-independent and fixed before policy execution.

---

## 4. Evidence semantics

Evidence dimensions remain:

```text
Authority: ESTIMATED < DERIVED < EXACT_OBSERVATION < AUTHORITATIVE
Status:    VALID | STALE | UNKNOWN | FAILED | AMBIGUOUS
Freshness: valid_until plus consumer max-age where defined
Scope:     explicit subject/action claims
```

The primary semantic action is request finalization because it requires `EXACT_OBSERVATION` Evidence scoped to the current Attempt and creates authoritative semantic commit.

The critical mixed case supplies:

```text
E1 = EXACT_OBSERVATION / VALID     / attempt a1
E2 = EXACT_OBSERVATION / AMBIGUOUS / attempt a1
```

Independent oracle:

```text
reconciliation          = AMBIGUOUS
semantic commit allowed = false
```

After the repair, the required observed behavior is:

```text
reconcile(finalize, {E1,E2}) = AMBIGUOUS
finalize_request(...)         = REJECTED / InsufficientEvidence
Request                       = RUNNING
Attempt authority             = CURRENT
```

No authoritative Output is committed.

---

## 5. Measurement boundary

The experiment keeps these concepts separate:

```text
Evidence set
!= policy-visible information
!= reconciliation outcome
!= semantic commit outcome
!= independent oracle
!= Gate violation
!= terminal O-class
```

B4 alone receives the frozen Evidence/reconciliation policy-information fields defined by C3. B0–B3 retain their existing information privileges. All policies still share the same repaired C1 semantic authority, so C4.5a is expected to remain a semantic null comparator across competent B0–B4.

The repaired C1 mechanism must not be relabeled as a unique B4 semantic advantage.

---

## 6. Metrics

Primary metrics:

```text
ACR  Ambiguous Commit Rate
SSER Silent Semantic Error Rate
```

ACR opportunities are exogenous paired events. The nine faulted scenarios therefore retain the same ACR EventIDs across B0–B4.

Expected repaired summary per policy:

```text
Policy  ACR   SSER
B0      0/9   0/9
B1      0/9   0/9
B2      0/9   0/9
B3      0/9   0/9
B4      0/9   0/9
```

Expected faulted-only outcome partition per policy:

```text
O1  0
O2  0
O3  9
O4  0
```

The non-faulted valid exact control remains O1 and is intentionally omitted from faulted-only outcome counts.

---

## 7. Anti-false-zero requirement

A private post-C1 divergence injection is retained. Starting from an ambiguous-only case that the repaired core correctly rejects, the injection forcibly installs an authoritative commit after rejection.

The measurement path must then record:

```text
ACR violation = true
O4            = true
```

This proves zero ACR/O4 in the repaired ordinary corpus is not caused by a dead metric path.

---

## 8. Provenance constraints

The post-repair branch deliberately reuses the behavior-independent C4.5a experiment implementation from preserved PR #71 while changing only assertions/documentation that describe the repaired observed outcome.

The historical record remains:

```text
pre-repair main:      0d93e9147a7e184b33588e49b63e7a8c2b39beca
pre-repair PR #71:    855c918dad2aff9d6235a436eba974b97d05fee3
pre-repair result:    ACR 1/9, SSER 1/9, O4 1
repair PR #73 head:   15a2ef451c89a5d530e37dc72f62a501251a7ddd
repair merge:         77411cb612c85db8f0171e315a762aa0f2044db4
post-repair result:   must be measured, not assumed
```

No post-repair result may be cited until exact-head CI executes the reproduced corpus.

---

## 9. Exit criterion

C4.5a closes only after the post-repair candidate demonstrates all of the following on an exact fenced head:

```text
same canonical 10-scenario corpus
same independent oracle and paired ACR EventIDs
mixed valid+ambiguous finalization fails closed
all nine faulted cases produce no authoritative semantic divergence
ACR = 0/9 for every policy
SSER = 0/9 for every policy
faulted O3 = 9 and O4 = 0 for every policy
valid exact control still commits normally
anti-false-zero injection still produces ACR + O4
Python 3.11 / 3.12 / 3.13 full repository CI green
bounded exact-head review finds no correctness or measurement-integrity blocker
SHA-fenced squash merge from repaired main
```

After that merge, issue #70 may close and C4.5 advances to C4.5b adversarial Evidence Safety.
