# C7.5b — Frozen Gate G2 Result

## Status

C7.5b authoritative execution is complete. This record freezes the P1/P4/P7 result generated from the predeclared C7.5a/C7.5b protocol and the reviewed execution path.

The first attempted stage-2 run (`34913661682`) is explicitly non-authoritative and invalidated: late review found provenance gaps before its result was accepted. Those defects were repaired before the authoritative result below was generated.

Replacement reviewed pre-result boundary:

```text
bf9af8fa54a2948c9fa4a77f3a1325ecbf5e2e68
```

Reviewed/wired runner boundary:

```text
05d4597ace4c04bdfe63cadf46bc086de5b01fbe
```

Authoritative generation commit:

```text
9ecb0d0f7c825d5da19c9b3814e4a1ce489b1b59
```

## Canonical identities

```text
workflow run                 34914426669
workflow artifact            10375273729
workflow ZIP SHA-256         0881f804bbbf47f0a39ff637efbe2b40cfc97c2d3b10c6dbf40c61fea65eddef

result JSON bytes            148115
result JSON SHA-256          ce3a34a13d885ee6ef5e3d9cbab392bb5ec9e69df89b7d143a8a7c348bb68bb6
scientific fingerprint       3bfcfa2a140757ebdfb9bf06d9e167efb6d93ffec5f3cae5ca8c712ff84e64bc

canonical Program rows       60160
Program-row bytes            53587267
Program-row SHA-256          95adba17f8048ecbca3bd96affef363e7366f664abe189bca1c7b116118ac534
Program-row schema           cadi.c7.5b.program-rows.canonical-jsonl.v1

committed manifest SHA-256   d3f13149e59bf11aae47a61655121d735df9a9a8d91cc3c0a51930fdf0a8123c
```

The authoritative workflow executed the complete sweep twice and required byte identity for the result JSON, Program-row JSONL, and compact summary before uploading the artifact.

## Coverage and integrity

The accepted row set is exhaustive for the frozen realizable surfaces:

```text
P1_DEEP_REUSE               48000 rows
P4_FANOUT_SHARED_PREFIX     10240 rows
P7_STATELESS_OVERHEAD        1920 rows
                            --------
TOTAL                       60160 rows
```

Each accepted hardware stratum contributes `30080` rows. Each B0–B4 policy contributes `12032` rows. All `60160` experiment keys are unique, all 64 frozen seeds map to 64 unique source records, all rows are efficiency-eligible, and semantic-violation row count is zero.

## H4 — recomputation-ratio phase space

Frozen decision:

```text
SUPPORTED_WITHIN_DECLARED_PHASE_SPACE
```

The complete 75-cell P1 map was evaluated independently for B0, B1, and B2. A cell supports H4 only when the paired 95% interval for `RR(B4) - RR(baseline)` is entirely below zero on both accepted hardware-model strata, with non-timing results invariant across strata.

Qualifying connected-region sizes are:

```text
B0: 20, 20, 20
B1: none
B2: 20, 20, 20
```

Each 20-cell component is one fixed-worker-count P1 surface spanning all positive reusable-prefix fractions over the five frozen session depths. Zero-reuse cells are null, as required.

Interpretation: B4 reduces recomputation relative to B0 and B2 across broad connected P1 regions. B1 remains a valid no-benefit/tie comparator. The result therefore does **not** support a universal B4-over-all-baselines claim.

## H7 — statefulness gradient

Frozen decision:

```text
SUPPORTED_WITHIN_DECLARED_PHASE_SPACE
```

The complete 75 P1 + 16 P4 timing map was evaluated against the predeclared B0 reference. A qualifying gradient requires at least three contiguous ordered levels, favorable paired timing intervals at every cell, a nondecreasing median per-Program benefit with at least one strict increase, and the same direction independently on both hardware-model strata.

The frozen result contains exactly 65 qualifying runs:

```text
P1 session_depth                 20
P1 reusable_prefix_fraction      45
P4 fanout_width                    0
P4 shared_prefix_fraction          0
                                  --
TOTAL                              65
```

The positive statefulness gradient is therefore specific to deep continuation/reuse structure. P4 fan-out remains informative negative/interaction evidence: it does not satisfy the predeclared monotone-gradient rule.

## P7 negative control

P7 passes on all six hardware×worker-count strata (`2 hardware × 3 worker-count cells`). For every seed and B0–B4 policy:

- semantic violations are zero;
- efficiency eligibility is `ELIGIBLE`;
- State Reuse Ratio is exactly zero;
- Recomputation Ratio is exactly zero;
- modeled Program Completion Time is exactly equal across policies for the same seed/hardware/cell.

This remains a modeled negative control under `ZERO_UNEVIDENCED_COMPONENT`; it is not a physical control-plane-overhead measurement.

## Independent raw-row adjudication

After the authoritative double-sweep passed, the uploaded artifact was independently audited from `program-rows.jsonl` without invoking the repository C7.5 finalizer.

The independent audit:

1. verified the uploaded ZIP, result JSON, and Program-row SHA-256 identities;
2. parsed all 60,160 JSONL rows and verified canonical JSON representation and unique complete experiment keys;
3. verified exact 64-seed/64-source coverage, hardware/policy cardinalities, eligibility, and zero semantic violations;
4. independently implemented the frozen SHA-256 bootstrap-index schedule, Type-7 percentile interval, H4 ratio-of-sums estimator, H7 ratio-of-summed-times estimator, median trend estimator, connected-component logic, gradient search, and P7 control checks;
5. reproduced all 225 H4 cell/comparator decisions exactly;
6. reproduced all 91 H7 cell decisions, with maximum numeric difference from the stored floating-point evidence of `8.881784197001252e-16`;
7. reproduced exactly the 65 stored H7 qualifying-run signatures;
8. independently obtained the same final Gate G2 classification.

## Gate G2

Closed imported evidence remains unchanged:

```text
H5 = SUPPORTED_WITHIN_DECLARED_PHASE_SPACE
H6 = EFFICIENCY_STRENGTHENED_WITHIN_DECLARED_PHASE_SPACE
```

New frozen decisions are:

```text
H4 = SUPPORTED_WITHIN_DECLARED_PHASE_SPACE
H7 = SUPPORTED_WITHIN_DECLARED_PHASE_SPACE
P7 = PASS
```

Applying the C7.5a truth table gives exactly:

```text
G2_A_STRONG_EFFICIENCY_SUPPORT
```

This is the final C7 Gate G2 efficiency classification.

## Scientific interpretation

The strongest publication-safe efficiency statement is not that B4 always wins. The result supports a narrower phase-space claim:

- continuity-aware information materially reduces recomputation in deep-reuse P1 workloads relative to B0/B2;
- modeled completion-time benefit grows along multiple P1 statefulness gradients on both accepted C6 hardware-model strata;
- B1 convergence and the absence of qualifying P4 gradients are retained as null/interaction evidence rather than hidden;
- H5 and H6 contribute independent retention and migration/failover efficiency regions already frozen in prior C7 slices.

The strongest unique semantic-correctness result remains the earlier C4.3 State-lineage result; C7.5 is an efficiency/phase-space result and does not reopen correctness adjudication.

## Evidence boundary

The final result remains bounded exactly as predeclared:

- source token lengths: pinned Mooncake/C7-admissible evidence;
- workload/session/fan-out/resource augmentation: P-SRC4 `TRACE_AUGMENTED` synthetic structure;
- timing: `SIMULATED_SOURCE_MODEL_DERIVED_P_SRC2`;
- no direct A100/H100 wall-clock measurement claim;
- no physical routing-overhead claim from P7;
- no production prevalence claim;
- no universal-superiority claim.

Any later change to the frozen scientific code, source-selection rule, bootstrap/statistical rules, comparator roles, or Gate G2 truth table requires a new protocol/result version and must preserve this result as prior evidence.
