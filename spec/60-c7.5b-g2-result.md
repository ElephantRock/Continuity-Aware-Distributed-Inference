# C7.5b — Frozen Gate G2 Result

## Status

C7.5b authoritative execution is complete. This record freezes the provenance-hardened P1/P4/P7 result generated under the predeclared C7.5a/C7.5b protocol.

The earlier candidate generated at `9ecb0d0f7c825d5da19c9b3814e4a1ce489b1b59` is retained only as **superseded, non-final evidence**. A late review found that its in-process checkout guard ignored non-ignored untracked files. The replacement path repaired that provenance defect and added `experiments/trace_workload.py` to scientific-parent fences without changing Program construction, estimators, bootstrap rules, comparators, support thresholds, gradient rules, or Gate G2 logic.

Frozen boundaries:

```text
C7.5a closure base             fcee10c5c647c9dfe11c7930987e331222fe124d
stage-1 scientific mechanics  bf9af8fa54a2948c9fa4a77f3a1325ecbf5e2e68
pre-result / runner review     4bd4a8d268356daf1c856512a34b1b5a02cd8a31
authoritative generation       823bcb1f6ad9178d21052eec81934fb0199e1efa
```

## Canonical identities

```text
workflow run                  34977721056
workflow artifact             10400103456
workflow ZIP SHA-256          8ebaba31df6013fb58e21aec330b88a2867139af5f3caeb527eb7687695447da

result JSON bytes             148115
result JSON SHA-256           f3eb3b8c5ee9e031aa9d3a1465e7e50c995cc6dab1020c3ee03662bfaa6765d7
summary JSON bytes            46862
summary JSON SHA-256          e20e2cfb517007ae197f4ecc13d25995f49e43721850a4a0b4402cc0e28a63bd
scientific fingerprint        1ff70aa4985743483ca897120e819889846576a66d320ec23674406919ba2b17

canonical Program rows        60160
Program-row bytes             53587267
Program-row SHA-256           63692ca2b5940649c83e8d3e9cd2794eb93521bbbbcfa57b77a2d71bac5474be
Program-row schema            cadi.c7.5b.program-rows.canonical-jsonl.v1

committed manifest SHA-256    774eb4e59ba61f254b6fd5d4b5db908649ca5ce42cb93d1b9e2296da3208b3d5
```

The authoritative workflow checked out exact generation SHA `823bcb1f6ad9178d21052eec81934fb0199e1efa`, required a clean repository including all non-ignored untracked files, proved the reviewed scientific/mechanics and parent trees unchanged, executed the complete sweep twice, and required byte identity for result JSON, Program-row JSONL, and compact summary before upload.

## Coverage and integrity

The accepted row set is exhaustive for the frozen realizable surfaces:

```text
P1_DEEP_REUSE               48000 rows
P4_FANOUT_SHARED_PREFIX     10240 rows
P7_STATELESS_OVERHEAD        1920 rows
                            --------
TOTAL                       60160 rows
```

Each hardware-model stratum contributes `30080` rows. Each B0–B4 policy contributes `12032` rows. The set covers exactly 64 seeds mapped to 64 unique frozen source records. All 60,160 rows are efficiency-eligible and the semantic-violation count is zero.

## H4 — recomputation-ratio phase space

Frozen decision:

```text
SUPPORTED_WITHIN_DECLARED_PHASE_SPACE
```

The complete 75-cell P1 map was evaluated independently for B0, B1, and B2 under the frozen paired ratio-of-sums bootstrap rule.

Qualifying connected-region sizes:

```text
B0: 20, 20, 20
B1: none
B2: 20, 20, 20
```

Each 20-cell region is one fixed-worker-count P1 surface spanning the five session-depth levels and the four positive reusable-prefix fractions. Zero-reuse cells remain null. B1 remains a valid no-benefit/tie comparator; this result does not imply B4 dominates every baseline.

## H7 — statefulness gradient

Frozen decision:

```text
SUPPORTED_WITHIN_DECLARED_PHASE_SPACE
```

The complete 75-cell P1 plus 16-cell P4 timing map was evaluated against the predeclared B0 reference. The result contains exactly 65 qualifying axis-aligned runs:

```text
P1 session_depth                  20
P1 reusable_prefix_fraction       45
P4 fanout_width                     0
P4 shared_prefix_fraction           0
                                   --
TOTAL                              65
```

Thus the positive statefulness-gradient evidence is specific to deep continuation/reuse. P4 fan-out is retained as interaction/null evidence because neither P4 axis satisfies the frozen monotone-gradient rule.

## P7 negative control

P7 passes all six hardware-model × worker-count strata (`2 × 3`). For every seed and B0–B4 policy, semantic violations are zero, efficiency eligibility is `ELIGIBLE`, State Reuse Ratio and Recomputation Ratio are exactly zero, and modeled Program Completion Time is exactly equal across policies for the same seed/hardware/cell.

P7 remains a modeled stateless convergence check under the frozen zero-unevidenced-component assumption. It is not a physical control-plane overhead measurement.

## Independent raw-row review

The uploaded `program-rows.jsonl` was independently re-adjudicated without invoking the repository C7.5 finalizer.

The audit:

1. verified ZIP/result/summary/row identities and the exact 60,160-row cardinality;
2. independently reproduced the frozen SHA-256 bootstrap-index schedule and Type-7 percentile interval;
3. recomputed all 225 H4 cell/comparator tests with exact support decisions and maximum stored-statistic absolute difference `1.1102230246251565e-16`;
4. recomputed all 91 H7 cells with exact favorable/non-favorable decisions and maximum stored-statistic absolute difference `8.881784197001252e-16`;
5. independently recovered exactly the same 65 H7 gradient signatures;
6. verified all six P7 control strata; and
7. independently obtained the same Gate G2 classification.

## Gate G2

Closed imported evidence is unchanged:

```text
H5 = SUPPORTED_WITHIN_DECLARED_PHASE_SPACE
H6 = EFFICIENCY_STRENGTHENED_WITHIN_DECLARED_PHASE_SPACE
```

New C7.5b decisions are:

```text
H4 = SUPPORTED_WITHIN_DECLARED_PHASE_SPACE
H7 = SUPPORTED_WITHIN_DECLARED_PHASE_SPACE
P7 = PASS
```

The frozen C7.5a truth table therefore yields:

```text
G2_A_STRONG_EFFICIENCY_SUPPORT
```

## Scientific interpretation

The publication-safe result is a phase-space statement, not universal superiority:

- continuity-aware information reduces recomputation relative to B0/B2 over broad connected deep-reuse P1 regions;
- modeled completion-time benefit increases along multiple P1 statefulness gradients on both accepted C6 hardware-model strata;
- B1 convergence and the absence of qualifying P4 gradients are retained as null/interaction evidence;
- H5 and H6 contribute independent retention and migration/failover efficiency regions already frozen in earlier C7 slices.

The strongest unique semantic-correctness result remains the earlier C4.3 State-lineage result. C7.5 is an efficiency/phase-space result and does not reopen correctness adjudication.

## Evidence boundary

The result remains bounded as predeclared:

- source token lengths: pinned Mooncake/C7-admissible evidence;
- workload/session/fan-out/resource augmentation: P-SRC4 `TRACE_AUGMENTED` synthetic structure;
- timing: `SIMULATED_SOURCE_MODEL_DERIVED_P_SRC2`;
- no direct A100/H100 wall-clock measurement claim;
- no physical routing-overhead claim from P7;
- no production-prevalence claim;
- no universal-superiority claim.

Any later change to frozen scientific code, source selection, statistical rules, comparator roles, support rules, or the Gate G2 truth table requires a new protocol/result version and must preserve this result as prior evidence.
