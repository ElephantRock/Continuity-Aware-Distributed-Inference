# C8.3a1 — C2 replay entry-point amendment

Status: pre-result repair under issue #180.

## Basis

Predecessor C8.3a merge:
`d8e05ef0fe4ab6bf469202b4a2476dcbb12eede5`

Predecessor protocol fingerprint:
`40780a318a5a692c713d7d27ded86d1f0fc6cd920d1c1ef82114889f9d9e099a`

`comparative_result_inspection = NONE` remains mandatory.

## Finding

C8.3b bounded review found that several C8.3a `c2_entry` descriptions named mechanisms more specifically than the parent code actually exposes. The scientific trace contract itself was not implicated: trace IDs, checkpoint order, expected normalized outcomes, semantic projection fields, C4 opportunity/violation definitions, C8 scripts, topology rules, and comparison derivation remain unchanged.

An initial suspicion that `REPLICA_EVICTION` was non-executable was corrected during review: `FaultScheduleReplayer` constructs `CrossLayerFaultInjector`, whose existing `evict_replica()` delegates to `ResourceModel.evict_replica()`.

## Amendment

Protocol v3 is an overlay on reviewed v2. It changes only the protocol schema and six concrete C2 entry descriptions:

- X1: `ContinuityAdapter` plus `FaultInjector.reorder_after` over frozen late-result/observation delivery;
- X3: deterministic simulator checkpoint plus `ContinuityCore.state_compatible` under policy-neutral State placement;
- X4: deterministic simulator plus `FaultInjector.reorder_after` plus C1 Binding observation;
- X5: deterministic simulator plus `ContinuityCore.reconcile` on frozen ambiguous Evidence;
- X6: `CrossLayerFaultInjector.evict_replica` plus `FaultScheduleReplayer` plus `FaultTrustOracle`;
- X7: `CrossLayerFaultInjector.fail_worker` plus `ResourceModel`, `FaultTrustOracle`, and the C1 migration interruption/commit path.

X2 remains unchanged.

The v3 implementation programmatically compares each amended trace with v2 after removing only `c2_entry`; any other difference fails closed.

## Evidence boundary

No authoritative C8.3b aggregate artifact has been produced. A draft C8.3b workflow attempt failed before artifact upload and is superseded/non-authoritative. No Gate G3 performance result is in scope.

## Exit

Pin the exact v3 protocol fingerprint only after candidate CI and bounded review are clean. Merge only when exact-head tests prove the amendment is mapping-only and every newly named mechanism exists.