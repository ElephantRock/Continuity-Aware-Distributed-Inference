# C4.5b — Adversarial S4 Evidence Safety EV0

Issue: #76  
Parent: #69  
Post-repair base: `849bcd7aac73404ffa836de2a30299253a7f2651`

## Purpose

C4.5b stress-tests Evidence safety beyond the C4.5a deterministic corpus while preserving the common C1 semantic authority and the frozen C3 policy-information contract.

This document records the complete scientific sequence rather than rewriting the original failure as a pass:

```text
pre-repair adversarial measurement
-> explicit structured-contradiction C1 repair
-> post-repair adversarial reproduction
```

The primary semantic action remains request finalization because it is correctness-sensitive, requires `EXACT_OBSERVATION` Evidence scoped to the current Attempt, and produces authoritative Request/Attempt commit state.

## Preserved pre-repair result

PR #77 at exact head:

`8875486236487b0bbd274056436134c40135d9f5`

was exact-head CI green and intentionally closed unmerged after exposing a common C1 contradiction defect.

Exact-head workflow `34053668779`:

```text
Python 3.11  538 passed in 23.10s
Python 3.12  538 passed in 27.13s
Python 3.13  538 passed in 26.09s
```

Controlled case `A1-CONTRADICTORY-VALID-EXACT-SAME-SCOPE` supplied two individually VALID exact observations over the same Attempt and terminal-outcome fact:

```text
attempt-terminal-outcome=SUCCEEDED
attempt-terminal-outcome=FAILED
```

Independent oracle:

```text
reconciliation = AMBIGUOUS
semantic commit allowed = false
```

Observed pre-repair C1:

```text
reconcile(finalize, set) = MATCHED
finalize_request(...)     = APPLIED
Request.status            = COMPLETED
Attempt.authority         = COMMITTED
```

Per B0-B4 policy sharing the common C1 authority:

```text
ACR  = 1/6
SSER = 1/6
O3   = 5 faulted operations
O4   = 1 faulted operation
```

Bounded exact-head measurement review `5126313451` passed for measurement integrity and required the artifact to remain unmerged.

## Repair gate

Issue #78 / PR #79 introduced explicit structured semantic-claim metadata:

```text
claim_key
claim_value
```

and made conflicting valid structured claims reconcile as `AMBIGUOUS`. Correctness-sensitive direct Evidence requirements (`finalize`, `consume_state`, `commit_migration`) fail closed on such contradictions without parsing the human-readable `claim` field.

PR #79 exact candidate:

`e40638ef2c4a3ba63b5aee74a5ffa6b39c2cfe55`

passed workflow `34054315600`:

```text
Python 3.11  532 passed in 15.92s
Python 3.12  532 passed in 23.42s
Python 3.13  532 passed in 26.13s
```

Bounded exact-head review `5126338321` found no substantive semantic/regression blocker. The repair was independently squash-merged as:

`849bcd7aac73404ffa836de2a30299253a7f2651`

The pre-repair PR #77 remains preserved closed/unmerged.

## Post-repair reproduction contract

The post-repair reproduction keeps the same 11 canonical case IDs, pressure families, independent oracle, fixed exogenous paired ACR EventIDs, B0-B4 ordering, positive controls, classification path, and direct provenance/set-integrity guards from PR #77.

The exact pre-repair experiment implementation blob:

`546c7a7ee83ca4fec4233c11b234751f2c5a424a`

is preserved byte-for-byte as:

`experiments/_evidence_safety_adversarial_pre_repair.py`

The public `experiments/evidence_safety_adversarial.py` wrapper changes only fixture construction/serialization needed to represent the semantic fact explicitly:

- A1 retains the same human-readable contradictory claims and adds `claim_key = attempt:a1:terminal-outcome` with values `SUCCEEDED` and `FAILED`;
- A2 retains the agreeing multi-Evidence control and gives both observations the same `claim_key` and `SUCCEEDED` value;
- all other Evidence construction delegates to the preserved pre-repair implementation;
- observed Evidence serialization additionally records `claim_key` and `claim_value`;
- manifest definitions, oracle outcomes, EventIDs, policy order, semantic classification, metric accounting, and summary logic remain in the exact preserved module.

This isolates the post-repair representation instrumentation from the behavior-independent measurement contract.

## Canonical adversarial manifest

```text
A1 CONTRADICTORY-VALID-EXACT-SAME-SCOPE
A2 IDENTICAL-VALID-EXACT-MULTI-CONTROL
A3 VALID-PLUS-AMBIGUOUS-WRONG-SCOPE
B1 VALID-UNTIL-EQUAL-NOW-CONTROL
B2 VALID-UNTIL-EPSILON-EXPIRED
B3 OLD-OBSERVATION-NO-EXPIRY-CONTROL
C1 AUTHORITATIVE-VALID-CONTROL
C2 DERIVED-ONLY-WITH-EXACT-SUPPORT
D1 GOOD-PLUS-WRONG-SCOPE-VALID-CONTROL
D2 WRONG-SCOPE-ONLY
E1 EMPTY-EVIDENCE-SET
```

Six cases are faulted and each retains one fixed exogenous paired ACR EventID. Five cases are non-faulted controls.

## Additional integrity guards

Direct tests retain the pre-repair requirements that:

```text
VALID DERIVED Evidence cannot be recorded from non-VALID support
non-DERIVED Evidence cannot carry derivation provenance
conflicting duplicate Evidence identity is rejected
Output cannot reference unknown Evidence
consume_state does not union partial state/replica Evidence scopes
commit_migration does not union partial Binding/epoch Evidence scopes
```

The repair-specific invariant suite separately covers structured-claim representation and contradiction semantics.

## Expected repaired result

For every policy B0-B4 sharing common C1 semantic authority:

```text
ACR  = 0/6
SSER = 0/6
```

Faulted-only outcome partition:

```text
O1  0
O2  0
O3  6
O4  0
```

A1 must reconcile `AMBIGUOUS` and reject finalization with Request still RUNNING and Attempt still CURRENT. A2 must remain a successful positive control. The private anti-false-zero injection must still be able to convert a real C1 rejection into ACR/O4 to demonstrate metric sensitivity.

## Baseline boundary

All B0-B4 retain common C1 semantic authority. B4 alone receives the frozen Evidence authority/status/freshness/reconciliation fields allowed by C3. No baseline bypasses the common safety mechanism, and no semantic comparator win is manufactured by disabling C1 for B0-B3.

If all competent baselines reproduce equivalent semantic correctness after the common C1 repair, C4.5b records a semantic null comparator rather than a unique B4 safety distinction.

## Exit rule

C4.5b closes only after the post-repair corpus passes exact-head Python 3.11/3.12/3.13 CI, bounded exact-head review finds no measurement-integrity blocker, and the reproduction is independently squash-merged.

C4.5b alone does **not** adjudicate H3/S4. C4.5c measured distributed EV1 remains mandatory before the Evidence-safety hypothesis component can be decided.
