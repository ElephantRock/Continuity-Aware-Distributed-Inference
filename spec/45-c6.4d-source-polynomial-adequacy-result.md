# C6.4d — Source-Polynomial v3 Equivalence Result

## Status

C6.4d terminates at the mandatory source-export equivalence fence with:

```text
representation_decision = INADEQUATE_REVISE_REPRESENTATION
adequacy_decision       = null
result_kind             = PRE_FRESH_EQUIVALENCE_FAILURE
```

This is a **representation/source-equivalence result, not a fresh-boundary adequacy result**. No C6.4c MAPE or maximum-APE result exists.

C6.4e simulator integration remains blocked.

## Frozen basis

C6.4c merged representation v3 at:

```text
041bf75102628da401b771845fd0b436f261cd7e
```

Representation:

```text
cadi.c6.cost-representation.v3.source-polynomial-composite+decode-step-affine
```

Pinned source:

```text
microsoft/vidur
commit abae7f63aa857300f5cdc6f5e0d27860cd24721b
model  meta-llama/Llama-2-7b-hf
hardware/network families: A100 80GB / A100-DGX, H100 80GB / H100-DGX
```

Accepted source-model protocol carried from C6.4b:

```text
protocol ID          cadi.c6.4b.vidur-linear-regression-source-model.v2
protocol fingerprint 9decab129b64921d59696c58242f0047ad986c76b82a22819eb93166b5d3a717
```

The numerical substrate remains frozen to CPython 3.12.14, Ubuntu 24.04 x86-64, NumPy 1.26.4, pandas 2.2.3, scikit-learn 1.5.2, scipy 1.14.1, joblib 1.4.2, threadpoolctl 3.5.0, fasteners 0.19, and OpenBLAS 0.3.23.dev with `OPENBLAS_CORETYPE=Haswell`, pthreads, and one thread.

## Canonical source-polynomial compilation

C6.4d exports the source-trained Vidur `PolynomialFeatures + LinearRegression` estimators into the canonical C6.4c polynomial schema. The exporter binds component identity, hardware identity, ordered source feature names, complete polynomial power basis, binary64 coefficients/intercept, source-selected hyperparameters, and source protocol/provenance.

Carried single-token decode remains unchanged and adequate from C6.3:

```text
A100 fixed  0.00954034447272924 seconds/output-token
A100 slope  3.7204476520608805e-07 seconds/context-token-step
H100 fixed  0.005806526349997292 seconds/output-token
H100 slope  2.1500094784162922e-07 seconds/context-token-step
```

State/memory remains independently source-derived from pinned Vidur:

```text
state_fixed_bytes       0
state_bytes_per_token   524288
memory_capacity_bytes   85899345920
```

## Review correction: historical boundary invalidation

The first C6.4d implementation instantiated Vidur's `LinearRegressionExecutionTimePredictor` through its ordinary constructor. The pinned implementation trains `_models` and then eagerly calls `_predict_from_models()`. That eagerly materialized source prediction tables containing C6.4c second-boundary component identities before the equivalence fence had been adjudicated.

Therefore the earlier claim that the C6.4c boundary remained untouched was incorrect. The boundary is permanently recorded as:

```text
status = INVALIDATED_BY_SUPERSEDED_PREDICTOR_MATERIALIZATION
source_model_predictions_were_materialized = true
contains_serialized_fresh_reference_timings = false
```

The affected superseded workflow runs are:

```text
34272077193
34306392038
34307451811
```

No fresh timing values from those tables were serialized into the scientific artifact, used to fit/revise v3, or used to compute an adequacy metric. Nevertheless, materialization is sufficient to invalidate the boundary as an unbiased future adequacy boundary. It must not be reused.

The review-hardened compiler now runs the exact pinned training path while suppressing `_predict_from_models()`, asserts that `_predictions` remains empty, and never enters a fresh-evaluation path for this invalidated boundary.

## Mandatory source-equivalence fence

The corrected fence compares canonical scalar polynomial evaluation with the trained pinned Vidur sklearn estimator over the **complete non-fresh source materialization domain**, excluding only C6.4c boundary component identities.

The equivalence budget remains the previously frozen 8 binary64 representable steps. The corrected implementation counts true IEEE-754 binary64 step distance rather than dividing an absolute difference by a local `ulp()`, which can undercount across binade boundaries.

The equivalence domain is recorded as:

```text
source_materialization_domain_excluding_c64c_boundary_component_identities
```

and the current execution asserts:

```text
fresh_boundary_component_predictions_consumed_current_execution = false
fresh_boundary_values_consumed = false
```

### Corrected result

| Hardware | Component | Samples | Violations over 8 ULP | Max binary64 step distance | Max absolute error (ms) | Decision |
| --- | --- | ---: | ---: | ---: | ---: | --- |
| A100 80GB | `attn_prefill` | 266224 | 742 | 59 | 1.7763568394002505e-15 | `FAIL` |
| A100 80GB | all other exported components | complete non-fresh source domains | 0 | <= 8 | — | `PASS` |
| H100 80GB | `attn_prefill` | 266224 | 16 | 14 | 8.881784197001252e-16 | `FAIL` |
| H100 80GB | `mlp_down_proj` | 4082 | 1 | 9 | 4.996003610813204e-16 | `FAIL` |
| H100 80GB | all other exported components | complete non-fresh source domains | 0 | <= 8 | — | `PASS` |

Both hardware families therefore fail the frozen source-equivalence prerequisite. These differences are numerically tiny in physical units, but the declared 8-step criterion cannot be relaxed after observation.

This is not a new source-model performance-accuracy failure. It is a failure of representation v3's frozen canonical arithmetic transcription to satisfy its own source-equivalence contract.

## Reproducibility

Review-hardened scientific run:

```text
34358336644
```

All four independent `compile-reproduction` jobs passed immutable checkout, pinned runtime/BLAS checks, pinned Vidur checkout, 23 exporter/v3 tests, two-run byte equality, and the corrected source-equivalence assertions. They reproduced the same compile fingerprint across runner images/hosts:

```text
compile fingerprint
e3f2df7b200e6aef1b5cfd69b78fab5bc92b351750c8f1f8001e74680ecbc0c7

A100 profile fingerprint
0f4a6ebe48f597ca821696b92e0193f9c321595bf5a8ed77dbe662a4cb1c5cca

H100 profile fingerprint
af74fa6b76304150c6d76d4044f30c0f7fd704f664f78e773b2a9f9540399bf0
```

The evaluate job then ran the corrected fail-closed record generation twice and required byte-identical output without evaluating the invalidated boundary.

## Canonical corrected record

```text
artifact
artifacts/c6.4d/source-polynomial-adequacy.json

artifact Git blob SHA-1
3a9a5c711d53da5a7b5e9f192bb6021bc962dc97

canonical corrected result commit
7bbdac20957e5347f4ba3862924609dac9137b90

scientific fingerprint
b132f7dcded7fac06900a909e6a3fd18558a97b61c58e1b5d7e8ab5ee33a56cf

result_kind
PRE_FRESH_EQUIVALENCE_FAILURE

adequacy_decision
null

representation_decision
INADEQUATE_REVISE_REPRESENTATION
```

For both hardware records:

```text
fresh_evaluation_status = NOT_EVALUATED_BOUNDARY_INVALIDATED
fresh_prefill           = null
fresh_transfer          = null
adequacy_decision       = null
```

## Boundary disposition

The C6.4c identities remain recorded for audit, but they are **not eligible for reuse as an untouched adequacy boundary**:

Cold prefill, input tokens:

```text
85, 202, 475, 897,
1026, 1333, 1622, 1623,
2371, 2373, 2476, 2636,
3352, 3353, 3402, 3899
```

Point-to-point transfer, bytes:

```text
2482176, 2637824, 3620864, 4513792,
8527872, 9314304, 14540800, 14729216,
19251200, 19611648, 23420928, 23609344,
26705920, 27295744, 32391168, 32825344
```

A later adequacy attempt must freeze a genuinely new untouched boundary before any source reference at that boundary is materialized.

## No post-hoc repair

C6.4d does not increase the 8-step budget, change polynomial bases or coefficients, reorder features, alter source-selected hyperparameters, change the numerical substrate, move boundary axes, or use materialized C6.4c values as a tuning signal.

The review correction changes only the provenance/accounting and fail-closed verification machinery: it prevents future eager materialization during compilation, expands equivalence coverage to the complete non-fresh materialization domain, computes the declared binary64 distance correctly, and distinguishes representation rejection from unevaluated adequacy.

## Consequence

```text
representation v3 source equivalence   FAIL
fresh adequacy                         NOT EVALUATED
C6.4c second boundary                  INVALIDATED / DO NOT REUSE
ordinary simulator integration         BLOCKED
next adequacy boundary                 MUST BE NEW AND UNTOUCHED
```

Any subsequent representation revision must be independently source-semantic and must establish a new untouched adequacy boundary before source timing values are observed.
