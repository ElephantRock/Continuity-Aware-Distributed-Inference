# C7.4c — Migration / Failover Efficiency Result

## Status

Frozen C7.4c result generated from the reviewed C7.4b mechanics at exact execution commit:

```text
4e9d9ad5fd119a266dfb9282274cd414354d0d72
```

Parent C7.4b merge:

```text
9227e36883af785aef1dc7d253404cee0e8675d1
```

Frozen identities:

```text
C7.1 protocol
706e0d5fff362a1eda8c906b957c914251c6e7949bac6be3c2c143ae21625474

C7.4a protocol
21601257767442204f1acfae4c6e2e42959e37701c9236a2d2b560a2dda486b6

C7.4b engine
2083e1a227330ea1a0ac57caf166cf8777865cbe6e3c6d259796cfd56fe02c32

C6 scientific fingerprint
cc1b62c4e38a1612720f601375c425c0bd7d67b4eff11b101bfdf87b79a7a61a

C6 artifact SHA-256
93990386135ecbf6fc38e579841eace3431a068b10ee59209fe6ad7084bd8889

C4.4 correctness merge
0d93e9147a7e184b33588e49b63e7a8c2b39beca
```

Canonical result identity:

```text
scientific fingerprint
a8d435456b1d784592eda96e3352190456fb9aeb34dda6c12c5a0edb58b54944

canonical JSON SHA-256
3550516d9d2355849f5d6a731d7577f906856ec94a60d8947ad81e8382e25e18
```

The complete canonical JSON is deterministically regenerated from the execution commit and the frozen parent identities. `artifacts/c7.4c/migration-efficiency-result-manifest.json` records its identity and complete support-region summary.

## Evidence boundary

C7.4c does not change the frozen protocol or mechanics. Timing remains:

```text
SIMULATED_SOURCE_MODEL_DERIVED_P_SRC2
```

Fault/scenario structure remains P-SRC4 deterministic synthetic stress. There is no direct physical A100/H100 measurement claim, no stochastic failure-rate claim, no artificial repeated-seed replication, and no bootstrap over deterministic fixed scenarios.

C4.4 correctness remains closed and independent. C7.4c adjudicates only the efficiency component of H6.

## Exhaustive coverage

The canonical artifact contains:

```text
Track A rows                  40
Track B rows                1200
Track B rankable rows       1000
partial-materialization rows 200
semantic-invalid Track B rows   0
```

Every row is bound to the exact execution SHA and frozen manifest/result identities. Partial materialization remains exactly:

```text
UNRANKED_NO_PARTIAL_TRANSFER_FRACTION_EVIDENCE
```

and contributes no efficiency support.

## Track A — P5 crossover

For each accepted C6 source-model stratum, all 20 frozen P5 cells classify as:

```text
TRANSFER_FASTER  20
RECOMPUTE_FASTER  0
TIE               0
```

This is a source-model result over the exact declared C6 domain. It is not a direct hardware measurement and, by the frozen C7.4a rule, Track A cannot independently support H6.

## Track B — support regions

The frozen connected-support rule yields six qualifying regions. Every qualifying region spans the complete 20-cell P5 grid and therefore exceeds the minimum connected-component size of two.

### MATCHED_PLANNED_MIGRATION

```text
Recovery-Time support region: 20 cells
Transfer-Volume support:       none
```

B3 and B4 both select the same successful transfer path under the common cost rule and common C1 authority. B4 is no worse than B3 and is faster than the conservative recompute baselines B0–B2. This region is valid under the predeclared comparator rule, but it is not a unique B4-over-B3 distinction.

### SOURCE_FAILURE_VALID_REMOTE

```text
Recovery-Time support region: 20 cells
Transfer-Volume support:       none
```

The surviving exact remote State is transferable for both B3 and B4 through common C1 authority. As above, B4 matches B3 and improves over conservative recompute baselines.

### DESTINATION_FAILURE_AFTER_TRANSFER_BEFORE_COMMIT

```text
Recovery-Time support:   none
Transfer-Volume support: none
```

B3 and B4 both pay completed transfer service before the predeclared destination failure prevents semantic commit; both then safely recompute. The frozen evaluation therefore gives B4 no efficiency support in this scenario.

### STALE_BINDING_RESIDUAL

```text
Recovery-Time support region:   20 cells
Transfer-Volume support region: 20 cells
```

This is a Continuity-specific distinction. B3 can see the exact residual State/location but remains Binding/reconciliation blind, so it selects transfer when cost-eligible; common C1 later fences the stale candidate and B3 pays transfer plus fallback recomputation. B4 sees the stale/reconciliation context before service and recomputes directly, avoiding the redundant transfer without weakening common C1 correctness.

### CONCURRENT_CANDIDATE_LOSER

```text
Recovery-Time support region:   20 cells
Transfer-Volume support region: 20 cells
```

This is the second Continuity-specific distinction. B3 is unable to distinguish the losing candidate from an authorizable transfer before service and pays a transfer that common C1 subsequently fences. B4 sees the reconciliation mismatch and avoids the non-authoritative transfer before service.

## H6 efficiency adjudication

The frozen decision is:

```text
EFFICIENCY_STRENGTHENED_WITHIN_DECLARED_PHASE_SPACE
```

The decision is supported by six qualifying connected regions, including four regions from the two scenarios where Binding/reconciliation information creates a B4-specific efficiency distinction relative to B3:

```text
STALE_BINDING_RESIDUAL       Recovery Time + Transfer Volume
CONCURRENT_CANDIDATE_LOSER   Recovery Time + Transfer Volume
```

No semantic violation was counted as support. No partial-materialization result was ranked. The negative destination-failure result is retained rather than discarded.

## Interpretation boundary

This result supports the efficiency component of H6 only within the declared deterministic C7.4 phase space and accepted C6 source-model representation. It does not establish real-cluster failure frequencies, queueing effects, concurrent-link contention, control-plane latency, or direct GPU wall-clock performance.

The already-closed C4.4 correctness result is not reopened or strengthened into a unique B4 correctness advantage: common C1 Binding authority remains the semantic safety mechanism for every comparator.
