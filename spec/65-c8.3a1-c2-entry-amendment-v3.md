# C8.3a1 — Repaired-parent cross-layer replay protocol amendment

Status: pre-result protocol freeze candidate.

Parent issue: #180 under C8.3 parent #175.

## Provenance

- predecessor C8.3a v2 merge: `d8e05ef0fe4ab6bf469202b4a2476dcbb12eede5`
- predecessor protocol fingerprint: `40780a318a5a692c713d7d27ded86d1f0fc6cd920d1c1ef82114889f9d9e099a`
- repaired C8.2.1 substrate merge: `d08283ec8e13567609b4b472ce6e45587d93d9ea`
- C8.1 protocol fingerprint: `616cc4daa4c167152875522767a8cc968da25c63d6acc099c265bc3554641cb6`
- `comparative_result_inspection = NONE`

The C8.2.1 repair normalizes downstream worker socket reset/broken-pipe/abort into the existing explicit `PROCESS_FAILURE` path. It does not change Continuity semantics, the C8.1 trace set, row/comparison schemas, or Gate G3 performance evidence.

## Amendment scope

C8.3a v3 preserves the reviewed v2 scientific trace contract and changes only:

1. the concrete `c2_entry` description for six traces so the text names mechanisms that actually exist and can be replayed; and
2. the protocol provenance payload, which explicitly binds the repaired C8.2.1 substrate merge.

The six corrected mappings are:

- X1: `ContinuityAdapter` plus deterministic `FaultInjector.reorder_after` over the frozen late-result/observation delivery path;
- X3: deterministic simulator checkpoint plus `ContinuityCore.state_compatible` under policy-neutral State placement;
- X4: deterministic simulator scheduling plus `FaultInjector.reorder_after` and C1 Binding observation;
- X5: deterministic simulator scheduling plus `ContinuityCore.reconcile` over frozen ambiguous Evidence;
- X6: `CrossLayerFaultInjector.evict_replica`, `FaultScheduleReplayer`, and independent `FaultTrustOracle` validation;
- X7: `CrossLayerFaultInjector.fail_worker`, `ResourceModel`, `FaultTrustOracle`, and the C1 migration interruption/commit path.

X2 remains unchanged.

## Scientific invariants preserved from v2

The v3 implementation programmatically proves equality with v2 for every trace field other than `c2_entry`. Therefore the following remain unchanged:

- all seven trace IDs and order;
- forbidden correctness metrics;
- checkpoint IDs and expected normalized outcomes;
- opportunity and violation definitions/denominators;
- semantic projection fields and state fingerprint semantics;
- raw normalization rules and forbidden-violation classification;
- C8 fault scripts, required message kinds, lifecycle actions, worker-generation counts, and restart requirements;
- row schema `cadi.c8.3.layer-row.v2`;
- comparison schema `cadi.c8.3.comparison.v2`;
- replay-execution-failure handling and rankability rules;
- comparison derivation and correctness-pass definition.

No correctness result is inspected or inferred during this amendment.

## Freeze procedure

The dedicated v3 gate must:

1. checkout the exact candidate head and prove a clean tree;
2. prove the predecessor v2 contract and C1/C2 semantics are unchanged;
3. prove no substrate/semantic drift after C8.2.1 merge `d08283ec...`;
4. run the v2 contract tests, v3 amendment tests, and C8 prototype conformance tests;
5. compute the canonical v3 protocol fingerprint with `comparative_result_inspection = NONE`;
6. literal-freeze that fingerprint in code/tests/workflow/spec;
7. rerun exact-head CI and bounded review before merge.

The final literal fingerprint is intentionally absent from this candidate document until the candidate payload is executed by exact-head CI.

## Supersession

The original C8.3a v2 protocol remains immutable historical provenance. Its workflow is converted to an archival self-integrity check and no longer fences later intentional C8 substrate repairs. C8.3a v3 becomes the authoritative parent for C8.3b execution only after this amendment merges.

## Exit

C8.3b remains blocked until v3 is literal-frozen, exact-head CI is green, bounded review has no blocker, and the amendment is merged. No authoritative seven-trace aggregate and no Gate G3 performance result may be generated before that point.
