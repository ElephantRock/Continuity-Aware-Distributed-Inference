# C6.5 — End-to-End EV3 Reproducibility and C6 Exit Review

## Status

Final C6 reproducibility and exit-review slice under umbrella issue #102.

Scientific base is exact C6.4g closure main:

```text
3e1fc2c81179f779774743aaf352e1c923110972
```

C6.5 does not fit another model, inspect a new calibration source, revise source coefficients, create a new holdout, or claim direct A100/H100 measurement. Its purpose is to verify that the accepted C6 scientific chain can be reproduced from frozen inputs and that ordinary simulator execution consumes only the accepted finite-domain artifact with the same declared evidence boundary.

The retained evidence class remains:

```text
SIMULATED_SOURCE_MODEL_DERIVED_P_SRC2
```

C7 remains blocked until C6.5 passes and C6 is explicitly closed.

## 1. C6 result lineage under review

C6.5 preserves the complete scientific history rather than flattening it into the final passing representation.

```text
C6.1   transparent affine mechanics/provenance contract
C6.2   pinned Vidur P-SRC2 source family
C6.3   affine projection/held-out evaluation
       -> INADEQUATE_REVISE_REPRESENTATION
C6.4a  source-semantic representation v2 freeze
C6.4b  fresh adequacy evaluation
       -> INADEQUATE_REVISE_REPRESENTATION
C6.4c  source-polynomial representation v3 + second boundary freeze
C6.4d  pre-fresh source-equivalence fence
       -> INADEQUATE_REVISE_REPRESENTATION
       -> C6.4c boundary permanently invalidated by superseded predictor materialization
C6.4e  source-kernel-preserving representation v4 + exhaustive protocol freeze
C6.4f  exhaustive finite-domain source-equivalence proof
       -> ADEQUATE_WITHIN_DECLARED_DOMAIN
C6.4g  dependency-free request-time runtime projection
C6.5   reproducibility + exit review
```

The negative C6.3/C6.4b/C6.4d results remain part of the evidence record. C6.5 must not describe them as superseded errors or remove their methodological consequences.

## 2. Frozen accepted scientific identity

The only representation eligible for C7 calibrated EV3 use is:

```text
representation
cadi.c6.cost-representation.v4.source-ordered-polynomial+source-linear-algebra+decode-step-affine

pinned source
microsoft/vidur@abae7f63aa857300f5cdc6f5e0d27860cd24721b

frozen exhaustive protocol fingerprint
adc341d4e3eddf72c82e3a0c7b139b703b9ed0e662a724870aa8d74abe1f7df8

scientific fingerprint
cc1b62c4e38a1612720f601375c425c0bd7d67b4eff11b101bfdf87b79a7a61a

canonical artifact
artifacts/c6.4f/exhaustive-source-equivalence.json

canonical artifact SHA-256
93990386135ecbf6fc38e579841eace3431a068b10ee59209fe6ad7084bd8889

decision
ADEQUATE_WITHIN_DECLARED_DOMAIN
```

The declared source-equivalent finite domains are:

```text
cold prefill input tokens       1..4096
point-to-point predictor tokens 1..4096
point-to-point public bytes     predictor_tokens * 8192
```

Every accepted C6.4f prefill and transfer point has zero binary64 step distance from the pinned source result. No out-of-domain generalization follows from this proof.

## 3. Carried decode and State/memory mechanics

C6.5 retains the separately adjudicated mechanics:

```text
single-token decode
  carried C6.3 affine primitive

multi-token decode
  m*fixed_per_output_token
  + slope_per_context_token_step * (m*n + m*(m-1)/2)

recompute
  same accepted prefill function over input_tokens - reusable_prefix_tokens

State
  state_fixed_bytes      = 0
  state_bytes_per_token  = 524288

memory capacity
  85899345920 bytes (80 GiB)
```

The decode source family was defined under `max_model_len = 4096` with positive decode context. The runtime therefore must fail closed for a positive decode request whose initial context is zero or whose generated sequence would exceed 4096 total tokens. C6.5 treats this as an implementation-closure requirement, not a new scientific timing result.

Canonical runtime constraint for `output_tokens > 0`:

```text
input_tokens >= 1
input_tokens + output_tokens <= 4096
```

This closes an out-of-domain path discovered during the C6.5 review of the C6.4g runtime surface.

## 4. Transfer-domain limitation

The accepted source transfer table ends at:

```text
4096 predictor tokens * 8192 bytes
= 33,554,432 bytes
```

With the carried Llama-2-7B State-size scalar:

```text
524,288 bytes / State token
```

an unsplit full-State transfer remains inside the accepted source-equivalent table only through:

```text
64 State tokens
```

This is a declared C6 limitation, not an extrapolation invitation.

C7 must therefore do one of the following for transfer-sensitive experiments:

1. keep each modeled validated transfer inside the accepted 33,554,432-byte domain;
2. introduce a separately reviewed composition rule whose scientific meaning is explicit; or
3. use a separately declared P-SRC4 sensitivity model for larger transfers and label those results synthetic rather than calibrated P-SRC2.

C6.5 must not silently chunk, extrapolate, or substitute generic network timing and call the result source-equivalent v4 transfer evidence.

## 5. Scientific reproducibility obligation

The C6.5 workflow independently reproduces the accepted C6.4f result from source.

It must:

1. check out the exact C6.5 candidate head;
2. install the C6.4e-frozen CPython/package set;
3. force and verify the accepted NumPy/OpenBLAS numerical substrate;
4. check out exact pinned Vidur source commit `abae7f63aa857300f5cdc6f5e0d27860cd24721b`;
5. run the exact C6.4f evaluator twice in separate Python processes;
6. require the two regenerated JSON artifacts to be byte-identical;
7. byte-compare regeneration with the committed canonical C6.4f artifact;
8. verify the frozen protocol fingerprint, scientific fingerprint, and artifact SHA-256.

A failed reproduction is a C6 exit failure. C6.5 may not repair representation data in response.

## 6. Ordinary runtime reproducibility obligation

The request-time runtime remains dependency-free.

C6.5 must verify under `python -S` that:

```text
simulator.inference_cost_runtime imports
NumPy is not loaded
sklearn is not loaded
accepted artifact identity constants are exact
```

It must then run a deterministic exit harness twice. The harness:

- loads only the canonical C6.4f artifact through `load_c64f_runtime_profiles()`;
- requires exactly the A100/H100 profiles;
- verifies complete 4,096-point prefill and transfer tables;
- evaluates explicit zero/interior/boundary-valid workloads;
- verifies the decode-domain fence;
- verifies the transfer-domain fence;
- emits canonical JSON;
- requires byte-identical replay.

The exit harness does not execute Vidur or a scientific fit. It validates deployment of already-adjudicated evidence.

## 7. Runtime control workloads

The C6.5 runtime replay uses three deterministic workloads.

### Zero

```text
input_tokens           0
output_tokens          0
reusable_prefix_tokens 0
state_tokens           0
```

Every modeled cost must be exactly zero.

### Interior

```text
input_tokens           32
output_tokens          4
reusable_prefix_tokens 20
state_tokens           1
```

This exercises accepted prefill, carried multi-token decode composition, recompute, State/memory, and an in-domain transfer.

### Upper accepted transfer boundary

```text
input_tokens           4095
output_tokens          1
reusable_prefix_tokens 4095
state_tokens           64
```

This exercises the largest accepted unsplit State transfer and a decode request ending exactly at the 4096-token model-length boundary.

The harness also requires fail-closed rejection of at least:

```text
input_tokens=0, output_tokens=1
input_tokens=4096, output_tokens=1
state_tokens=65
```

These are domain checks, not new timing evidence.

## 8. Review findings carried into exit

C6.5 explicitly records two review findings.

### R1 — Decode domain fence required

C6.4g correctly fenced prefill and transfer domains but did not yet fence the carried decode primitive/composition to the source model's positive-context / 4096-token length boundary. C6.5 must add this guard and regression tests before C6 can exit.

### R2 — Validated transfer range is intentionally narrow

The accepted v4 transfer table covers at most 33,554,432 bytes. This does not invalidate C6 because the runtime fails closed rather than extrapolating, but it materially constrains calibrated transfer-sensitive C7 experiments. C7 must preserve the limitation or introduce separately classified evidence.

No other C6.5 review finding currently requires scientific-model revision.

## 9. C6 exit criterion

C6 may close only if the final exact-head PR evidence establishes all of the following:

```text
C6.4f scientific artifact reproduces byte-for-byte from pinned source
frozen protocol/scientific/artifact fingerprints reproduce exactly
request-time runtime remains dependency-free
runtime projection accepts only the exact canonical C6.4f artifact
prefill/decode/recompute/transfer/State/memory outputs are deterministic
decode and transfer source domains fail closed
runtime replay is byte-identical
all C6.5 tests and deterministic-core CI are green
negative historical C6 results remain preserved
C6.4c boundary remains explicitly invalidated
no direct-hardware or out-of-domain claim is introduced
```

Passing C6.5 establishes an EV3 modeled inference-cost substrate suitable for C7 **within the declared evidence domains and limitations**. It does not establish H4, H5, H6, or H7 performance conclusions by itself.
