# C6.4f — Exhaustive finite-domain source-equivalence proof

## Status

Execution slice for the C6.4e-frozen representation-v4 protocol. This document is intentionally result-free until the exact-source workflow produces the canonical artifact.

Base merge:

```text
C6.4e merge 91e442260e33ad0b5e485777faf0becff2295635
```

Frozen representation:

```text
cadi.c6.cost-representation.v4.source-ordered-polynomial+source-linear-algebra+decode-step-affine
```

Frozen protocol fingerprint:

```text
adc341d4e3eddf72c82e3a0c7b139b703b9ed0e662a724870aa8d74abe1f7df8
```

No new holdout is created. The C6.4c boundary remains permanently:

```text
INVALIDATED_BY_SUPERSEDED_PREDICTOR_MATERIALIZATION
```

C6.4f is an exhaustive equivalence proof over the already-consumed finite declared source domain, not fresh validation and not an out-of-domain generalization claim.

## Exact-source execution

For A100 and H100 independently, the evaluator must:

1. assert the frozen C6.4e protocol fingerprint before constructing any Vidur predictor;
2. verify the exact pinned Vidur commit, behavioral/state-source blobs, raw calibration artifact identities, installed sklearn 1.5.2 source blobs, and accepted fixed numerical runtime;
3. instantiate the exact pinned `LinearRegressionExecutionTimePredictor` under the accepted C6.4b v2 predictor/scheduler configuration;
4. derive every v4 record directly from the fitted predictor component mapping through `export_source_ordered_polynomial()`;
5. materialize v4 with the same source batch shapes used by Vidur: one 4096-row frame for compute/send_recv and one 266240-row frame for `attn_prefill` with kv-cache outer / prefill-chunk inner order;
6. compare request-level source and v4 cold-prefill seconds on every input-token axis 1..4096;
7. compare source and v4 point-to-point transfer seconds on every predictor-token axis 1..4096, with public bytes exactly `predictor_tokens * 8192`;
8. revalidate the carried C6.3 decode primitive and source-derived state/memory mechanics without refitting.

The source predictor is the oracle for this slice. The v4 path does not call sklearn estimator objects after export.

## Oracle

Every point must satisfy:

```text
binary64 representable-step distance <= 8 ULP
```

The report also records:

```text
MAPE    <= 0.05
max APE <= 0.10
```

These aggregate guards cannot excuse a single ULP violation. All source and v4 values must be finite and non-negative. Parent PASS requires A100 and H100 to pass prefill, transfer, carried decode, and state/memory checks.

A PASS means source-equivalent within the finite declared source domain. It does not turn the source-derived model into direct physical accelerator measurement and does not claim validity outside that domain.

## Reproducibility

The canonical result must be generated under:

```text
CPython           3.12.14
NumPy             1.26.4
pandas            2.2.3
scikit-learn      1.5.2
SciPy             1.14.1
joblib            1.4.2
threadpoolctl     3.5.0
fasteners         0.19
OpenBLAS          0.3.23.dev
OPENBLAS_CORETYPE Haswell
threading layer   pthreads
BLAS threads      1
```

The workflow runs the evaluator twice from the same exact repository and Vidur SHAs and requires byte-identical JSON before exposing the candidate artifact. A later acceptance step must record the canonical scientific fingerprint and, before merge, verify reproducibility on the final exact head.

## No-repair rule

Once the exhaustive source result is observed, C6.4f must not change coefficients, hyperparameters, polynomial order, request arithmetic, materialization shape, source domain, axes, numerical runtime, ULP budget, or aggregate guards in response to residuals. No clipping, smoothing, rounding repair, refitting, axis deletion, or post-hoc narrowing is permitted.

If v4 fails, the negative result is the result. C6.4g simulator integration remains blocked unless C6.4f passes.

## Result record

Pending exact-source execution. The canonical result artifact path is reserved as:

```text
artifacts/c6.4f/exhaustive-source-equivalence.json
```

This section must be updated only after the workflow produces and reproduces the exact-source artifact.
