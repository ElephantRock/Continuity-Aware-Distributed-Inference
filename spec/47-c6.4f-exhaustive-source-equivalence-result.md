# C6.4f — Exhaustive finite-domain source-equivalence proof

## Status

**PASS — ADEQUATE_WITHIN_DECLARED_DOMAIN.**

C6.4f executed the C6.4e-frozen representation-v4 protocol against the exact pinned Vidur source over the complete finite declared prefill and point-to-point transfer domains. The canonical result is committed at:

```text
artifacts/c6.4f/exhaustive-source-equivalence.json
```

Base merge:

```text
C6.4e merge 91e442260e33ad0b5e485777faf0becff2295635
```

Frozen representation:

```text
cadi.c6.cost-representation.v4.source-ordered-polynomial+source-linear-algebra+decode-step-affine
```

Frozen protocol fingerprint:

```text
adc341d4e3eddf72c82e3a0c7b139b703b9ed0e662a724870aa8d74abe1f7df8
```

Canonical scientific fingerprint:

```text
cc1b62c4e38a1612720f601375c425c0bd7d67b4eff11b101bfdf87b79a7a61a
```

Canonical artifact SHA-256:

```text
93990386135ecbf6fc38e579841eace3431a068b10ee59209fe6ad7084bd8889
```

Evidence class remains:

```text
SIMULATED_SOURCE_MODEL_DERIVED_P_SRC2
```

This is source-model equivalence evidence, not direct physical A100/H100 measurement.

## Boundary accounting

No new holdout was created. The C6.4c second boundary remains permanently:

```text
INVALIDATED_BY_SUPERSEDED_PREDICTOR_MATERIALIZATION
```

The canonical result records:

```text
validation_mode                    EXHAUSTIVE_FINITE_DECLARED_DOMAIN_EQUIVALENCE
replacement_fresh_boundary         null
holdout_claim                      false
out_of_domain_generalization_claim false
post_hoc_repairs                   []
```

C6.4f therefore establishes equivalence only inside the finite declared source domain. It does not restore the invalidated C6.4c boundary and makes no out-of-domain generalization claim.

## Exact-source execution

For A100 and H100 independently, the evaluator:

1. asserted the frozen C6.4e protocol fingerprint before constructing source predictors;
2. verified the exact pinned `microsoft/vidur@abae7f63aa857300f5cdc6f5e0d27860cd24721b` commit, behavioral/state-source blobs, raw calibration artifact identities, sklearn 1.5.2 source blobs, and fixed numerical runtime;
3. instantiated the exact pinned `LinearRegressionExecutionTimePredictor` under the accepted C6.4b v2 predictor/scheduler configuration;
4. derived every v4 source record directly from the fitted predictor component mapping through `export_source_ordered_polynomial()` and its content binding;
5. materialized v4 using the source batch shapes frozen in C6.4e: 4096 rows for compute/send_recv and 266240 rows for `attn_prefill`, with kv-cache outer / prefill-chunk inner ordering;
6. compared request-level source and v4 cold-prefill seconds for every input-token axis `1..4096`;
7. compared source and v4 point-to-point transfer seconds for every predictor-token axis `1..4096`, with public bytes exactly `predictor_tokens * 8192`;
8. revalidated the carried C6.3 single-token decode primitive and source-derived state/memory mechanics without refitting.

The source predictor is the oracle for this slice. The v4 evaluation path does not use sklearn estimator objects after export.

## Exhaustive equivalence result

The frozen pointwise source-equivalence oracle is:

```text
binary64 representable-step distance <= 8 ULP at every point
```

The aggregate report guards are:

```text
MAPE    <= 0.05
max APE <= 0.10
```

They cannot excuse any ULP violation.

The canonical result is:

| Hardware | Family | Points | Max ULP | ULP violations | MAE (s) | MAPE | Max APE | Decision |
| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: | --- |
| A100 80GB | cold prefill | 4096 | 0 | 0 | 0.0 | 0.0 | 0.0 | PASS |
| A100 80GB | point-to-point transfer | 4096 | 0 | 0 | 0.0 | 0.0 | 0.0 | PASS |
| H100 80GB | cold prefill | 4096 | 0 | 0 | 0.0 | 0.0 | 0.0 | PASS |
| H100 80GB | point-to-point transfer | 4096 | 0 | 0 | 0.0 | 0.0 | 0.0 | PASS |

Every compared source and v4 value is finite and non-negative. All zero-reference cases satisfy the exact-zero requirement. No point exceeds the 8-ULP budget.

### Frozen identities

A100 80GB:

```text
C6.3 source corpus fingerprint  f1ff902e51952273e8788a79b2c7309048e224b26d6aa19e49726ab4416a7e32
v4 profile fingerprint          d5804e25426f7849e77c865de22aff72ddc9c805c54dee51e55b2a8641f332a3
materialized-domain fingerprint 31aa9af1a71d40a101867c8dc1ef69c4d8a3cb11719441622efee7c593ff15dd
```

H100 80GB:

```text
C6.3 source corpus fingerprint  0b379d376b30c1152e4d0d429eab6458ef85f91ee130f5e5969e1b757f8e7077
v4 profile fingerprint          37ce4f835054573034fc6ebbc5609ff239d7a5ec5539e3dd54c5dc48584a221c
materialized-domain fingerprint 28ad79027fd68d54ee5bc781abb810532759bfe31dca88bfce0ce40eb4fc3ce8
```

## Carried decode and state/memory

The C6.3 single-token decode families remain `ADEQUATE_WITHIN_DECLARED_DOMAIN`, and their carried coefficients match the frozen C6.3 values exactly. Multi-token decode composition is unchanged.

State/memory provenance also passes unchanged for both hardware profiles:

```text
state_fixed_bytes      0
state_bytes_per_token  524288
memory_capacity_bytes  85899345920  (80 GiB)
```

These values remain source-derived and provenance-bound.

## Reproducibility

The canonical result was generated under the C6.4b/C6.4e fixed numerical substrate:

```text
CPython           3.12.14
NumPy             1.26.4
pandas            2.2.3
scikit-learn      1.5.2
SciPy             1.14.1
joblib            1.4.2
threadpoolctl     3.5.0
fasteners         0.19
OpenBLAS          0.3.23.dev
OPENBLAS_CORETYPE Haswell
threading layer   pthreads
BLAS threads      1
```

Each acceptance run executes the exact-source evaluator twice in separate Python processes and requires byte-identical output. Final-head CI must additionally byte-compare the regenerated result against the committed canonical artifact and verify both the scientific fingerprint and artifact SHA-256 above. This converts the first result capture into an immutable, reproducible acceptance record rather than allowing later result-dependent repair.

## No-repair rule

No coefficient, hyperparameter, polynomial order, request arithmetic, materialization shape, source domain, axis, numerical runtime, ULP budget, or aggregate guard was changed in response to the observed exhaustive result. No clipping, smoothing, rounding repair, refitting, axis deletion, or post-hoc narrowing is present.

Negative results would have remained valid results. In this execution, representation v4 passes the frozen finite-domain source-equivalence protocol exactly.

## C6 ladder

```text
C6.4e  v4 source-kernel representation/protocol  CLOSED
C6.4f  exhaustive finite-domain source proof     PASS / this result
C6.4g  simulator integration                     UNBLOCKED after C6.4f merge
C6.5   reproducibility + C6 exit review           PENDING
```

C7 broad efficiency evaluation remains blocked until C6 exit.
