# C6.4e — Source-kernel representation v4 and exhaustive finite-domain protocol

## Status

Protocol/representation freeze only. This slice does **not** evaluate any new pinned-Vidur timing or source-model prediction.

C6.4d closed at merge `4b937cd65c7731981a04524279d43a74f3032dc8` with:

```text
representation_decision = INADEQUATE_REVISE_REPRESENTATION
adequacy_decision       = null
result_kind             = PRE_FRESH_EQUIVALENCE_FAILURE
```

The failure was numerical transcription: representation v3 reordered/folded source polynomial terms and used scalar multiplication plus `math.fsum`, while the pinned source evaluates `PolynomialFeatures` in source order and `LinearRegression.predict` as ndarray matrix-vector multiplication plus intercept. The corrected C6.4d fence found binary64 differences above the frozen 8-ULP transcription budget, so v3 was rejected before an adequacy result could exist.

## Boundary accounting

The C6.4c second boundary is permanently:

```text
INVALIDATED_BY_SUPERSEDED_PREDICTOR_MATERIALIZATION
```

It is audit history only. It may never again be described as fresh, untouched, or held out.

C6.4d source-equivalence work also evaluated the complete finite materialization domain required for this pinned TP1/PP1 source profile. Consequently there is no scientifically honest third untouched subset inside the same finite `1..4096` prefill/transfer lookup domain. C6.4e therefore freezes an **exhaustive finite-domain equivalence protocol**, not another sampled holdout.

This makes no claim of generalization beyond the declared source domain.

## Representation v4

Representation ID:

```text
cadi.c6.cost-representation.v4.source-ordered-polynomial+source-linear-algebra+decode-step-affine
```

Each source polynomial record preserves:

```text
ordered input feature names
exact sklearn PolynomialFeatures powers_ row order
bias/constant row exactly when upstream emits it
coefficient vector in that exact order
regression intercept as a separate source value
selected upstream grid-search hyperparameters
source protocol fingerprint
explicit numerical evaluation-kernel identifier
```

No constant folding, monomial reordering, `math.fsum`, coefficient repair, clipping, smoothing, or source-value-dependent arithmetic selection is permitted.

### Numerical kernel

The scientific projection reuses the accepted C6.4b v2 numerical substrate:

```text
CPython           3.12.14
NumPy             1.26.4
scikit-learn      1.5.2   [source semantics / C6.4f oracle only]
threadpoolctl     3.5.0
OpenBLAS          0.3.23.dev
OPENBLAS_CORETYPE Haswell
threading layer   pthreads
BLAS threads      1
```

The v4 evaluator does not require sklearn estimator objects. It reproduces the frozen dense `PolynomialFeatures.transform` multiplication schedule using NumPy, then evaluates the serialized regression as:

```text
X_transformed @ coef + intercept
```

The relevant frozen source-code identities are:

```text
scikit-learn 1.5.2 sklearn/preprocessing/_polynomial.py
    git blob f4c9fb032cfb05b1e97640ff000116e8b3b8cf87

scikit-learn 1.5.2 sklearn/linear_model/_base.py
    git blob 02a72355e36029f8339838a03949a2c83f8b0654

microsoft/vidur@abae7f63aa857300f5cdc6f5e0d27860cd24721b
vidur/entities/execution_time.py
    git blob a5100f86b7e4d885ff62a5a66f845682a998add8
```

The last identity is important because source fidelity is request-level, not merely component-level. Cold-prefill composition therefore follows Vidur's ordinary left-associative nesting:

```text
attention = pre_proj + post_proj + rope + kv_save + decode + attn_prefill + tp_comm + attn_norm
mlp       = up_proj + down_proj + act + tp_comm + mlp_norm
block     = attention + mlp + add
model_s   = (block * layers + pp_comm) * 1e-3
```

For the frozen TP1/PP1 cold-prefill case, decode/TP/PP contributions are zero and `layers=32`. Compute-component lookup keeps Vidur's existing 8-token rounding; KV-save and attention retain exact request state.

## Carried mechanics

C6.4e does not revise independently adjudicated mechanics:

```text
single-token decode     carried C6.3 affine primitive
multi-token decode      m*fixed + slope*(m*n + m*(m-1)/2)
recompute               same prefill function on missing prefix
state bytes/token       source-derived C6.4d value
memory capacity         source-derived 80 GiB device capacity
transfer byte mapping   8192 bytes / predictor token
```

All remain provenance-bound. No physical A100/H100 measurement is created by this slice.

## C6.4f exhaustive source-equivalence protocol

C6.4f is frozen to evaluate every public axis in the finite declared source domain, independently for A100 and H100:

```text
cold prefill input tokens          1..4096 inclusive  (4096 points)
point-to-point predictor tokens    1..4096 inclusive  (4096 points)
point-to-point public bytes        predictor_tokens * 8192
```

Recompute is the same prefill function and is therefore exhaustively covered by the prefill proof. Decode remains the already-adjudicated carried primitive/composition. State/memory remain algebraic sourced mechanics.

### Oracle

The source-equivalence requirement is unchanged:

```text
binary64 representable-step budget <= 8 ULP at every point
```

The existing adequacy guards remain reportable:

```text
MAPE    <= 0.05
max APE <= 0.10
```

but they cannot excuse any point exceeding the source-equivalence ULP budget. A hardware profile passes only when every exhaustive point passes the transcription fence, values are finite/non-negative, provenance/fingerprints are intact, decode remains adequate, and state/memory mechanics remain valid. Parent pass requires A100 and H100.

C6.4f results are `SIMULATED_SOURCE_MODEL_DERIVED_P_SRC2`; they are not direct accelerator measurements.

## Anti-leakage / no-repair rule

C6.4e and C6.4f fail review if any already-consumed axis is re-labelled as fresh, v4 arithmetic is selected from residual minimization instead of pinned source semantics, coefficients or hyperparameters are changed after exhaustive results, source predictions are clipped/rounded/smoothed/refit, runtime/source identity drifts silently, or exhaustive finite-domain equivalence is used to claim validity outside the frozen source domain.

Negative results remain valid results. If v4 fails C6.4f, record the failure; do not repair it against the observed exhaustive output.

## C6 ladder after this freeze

```text
C6.4d  v3 source compilation/equivalence       CLOSED / representation rejected
C6.4e  v4 source-kernel representation/protocol THIS SLICE
C6.4f  exhaustive finite-domain source proof    BLOCKED until C6.4e merge
C6.4g  simulator integration                    BLOCKED until C6.4f PASS
C6.5   reproducibility + C6 exit review          PENDING
```

C7 broad efficiency evaluation remains blocked until C6 exit.
