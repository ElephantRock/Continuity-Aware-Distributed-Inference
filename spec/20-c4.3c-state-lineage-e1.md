# 20 — C4.3c State-Lineage EV1
## Distributed CPU S2 Validation Contract

**Project:** Continuity-Aware Distributed Inference  
**Milestone:** C4.3c — S2 State-Lineage Safety, EV1  
**Tracking:** #60 under #55 / umbrella #44  
**Base:** `e0541ce57ebe6f5951003b6d7dddf531831cf99b`  
**Status:** IN PROGRESS — initial real-process candidate

---

# 1. Research boundary

C4.3c is the final bounded S2 evidence slice.

It validates State-lineage behavior using real CPU worker processes and real IPC while keeping the closed C1/C2/C3 semantics and B0–B4 information contracts unchanged.

The safety property remains:

```text
Consume(x, κ) => Compatible(x, κ)
```

The measured execution boundary is:

```text
policy placement
!= worker execution rule
!= physical State consumption
!= application result production / detection / recovery
!= authoritative terminal finalization
```

The coordinator owns Continuity semantic authority. Worker processes own only physical State presence and physical execution.

---

# 2. EV1 topology

Each scenario starts two `spawn`-based worker processes:

```text
coordinator
├── Pipe <-> w1 process
└── Pipe <-> w2 process
```

Each worker has:

```text
independent PID
process-local State-ID -> ReplicaID map
RESET / INSTALL / EVICT / EXECUTE commands
SHA-256 CPU work
measured process CPU time
measured wall timestamps
```

The worker does not receive:

```text
Continuation ancestry
producer Attempt authority
State lineage compatibility
ground-truth compatibility
```

It applies only the common physical rule:

```text
consume the exact declared candidate only if that candidate is physically local
```

This preserves ordinary physical State validation without giving B0–B3 the B4 causal-lineage mechanism.

---

# 3. Mandatory corpus

The EV1 corpus contains seven scenarios.

```text
E1-S2-A-WRONG-SIBLING
E1-S2-B-SUPERSEDED-PRODUCER
E1-S2-C-ABANDONED-RESIDUAL
E1-S2-D-SIMILAR-DIFFERENT
E1-S2-E-DERIVED-INVALID-DEPENDENCY
E1-S2-F-VALID-ANCESTOR-CONTROL
E1-S2-G-POST-OBSERVATION-EVICTION
```

Five cases are lineage-incompatible and physically attractive at `w1`.

One is a non-faulted valid-ancestor reuse control.

One begins with a compatible State advertised at `w1`, then physically evicts it after placement observation and before execution. The semantic directory remains stale intentionally; the real worker must report no consumption and recomputation rather than synthetic reuse.

---

# 4. Ground truth and oracle

Each policy trial creates a fresh C1 semantic runtime.

The independent oracle is inherited from C4.3b and does not call:

```text
ContinuityCore.is_ancestor(...)
ContinuityCore.state_compatible(...)
```

It independently checks:

```text
ExecutionContext linkage
Continuation ancestry
StateValidity
producer Attempt authority
committed producer Request identity
recursive derived_from dependency compatibility
```

The exact C1 result must agree with the independent oracle before execution.

The oracle result is not visible to the worker process or weaker policies.

---

# 5. Physical State evidence

Before each policy execution:

```text
RESET w1
RESET w2
INSTALL candidate on w1
```

When the source scenario has a compatible alternative, it may also be physically installed on `w2`, but the closed execution directive remains scoped to the declared candidate State.

For the post-observation eviction case:

```text
scheduler observes candidate at w1
policy decides placement
EVICT candidate from real w1 process
worker executes
```

The experiment records the actual `EVICTED` response and the worker's execution-time local-State snapshot.

---

# 6. Real CPU execution

Every selected worker performs SHA-256 work with:

```text
minimum fixed rounds          5,000
minimum process CPU seconds   0.003
```

The worker returns:

```text
worker PID
execution start/completion wall timestamps
process CPU seconds
wall execution seconds
CPU digest
local State snapshot
consumed StateID / ReplicaID or None
consumption EventID or None
application result token
recovery disposition
```

A trial aborts if the measured process-CPU floor is not reached.

---

# 7. Application-result independence

The application profile remains policy-hidden and separate from State compatibility.

The five incompatible cases reuse the already-closed profiles:

```text
wrong sibling            WRONG_UNDETECTED
superseded producer      DETECT_AND_RECOMPUTE
abandoned residual       WRONG_UNDETECTED
similar-but-different    CORRECT_RESULT
derived invalid dep      WRONG_UNDETECTED
```

Therefore real incompatible State consumption can produce:

```text
O4  silent wrong result
O2  detected + recomputed correct result
O1  coincidentally correct result
```

WBRR/WSCR do not mechanically determine SSER.

Every worker result becomes `EXACT_OBSERVATION` Evidence, is attached to a terminal Output, and is finalized through C1 before O-class is recorded.

---

# 8. Metric contract

## WBRR

Three exogenous paired opportunities are fixed before policy execution:

```text
wrong sibling
abandoned residual
similar-but-different
```

A WBRR violation requires an actual worker-process candidate consumption.

## WSCR

WSCR remains policy-derived.

The denominator exists only when a faulted operation emits a real measured State-consumption event.

## SSER

SSER uses the six faulted operations:

```text
five incompatible-State cases
+
post-observation physical eviction
```

The valid-ancestor control is non-faulted.

---

# 9. Candidate expected result

If the real-process implementation preserves the closed policy behavior, the candidate should produce:

```text
Policy  WBRR   WSCR   SSER
B0      0/3    0/0    0/6
B1      3/3    5/5    3/6
B2      3/3    5/5    3/6
B3      3/3    5/5    3/6
B4      0/3    0/0    0/6
```

Expected faulted O-class partition for B1–B3:

```text
O1  1
O2  2
O4  3
```

Expected B0/B4 faulted partition:

```text
O2  6
O4  0
```

This is an expectation until exact-head CI validates the executable summary.

As in C4.3a/C4.3b, B4 WSCR `0/0` is null coverage rather than a denominator pass. B4 must also consume the real valid-ancestor control, and a separate injected execution-plane fault must prove that an actual B4 incompatible consumption increments WBRR/WSCR and reaches the appropriate O-class.

---

# 10. Exit criterion

C4.3c may merge only after:

```text
7-case EV1 corpus reproducible
real two-process worker topology verified
real process CPU floor verified
physical install/eviction events preserved as measured evidence
independent oracle == exact C1 compatibility
WBRR/WSCR derived from actual worker consumption only
post-observation eviction produces no fake consumption
B4 real valid-ancestor reuse control succeeds
injected real B4 incompatible consumption produces nonzero Gate metrics
Python 3.11 / 3.12 / 3.13 CI green on exact head
bounded exact-head review finds no substantive correctness/measurement issue
```

After C4.3c closes, #55 must adjudicate H2 from C4.3a + C4.3b + C4.3c evidence.
