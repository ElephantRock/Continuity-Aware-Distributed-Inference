# 19 — C4.3b State-Lineage Adversarial EV0
## Deterministic S2 Adversarial Corpus Contract

**Project:** Continuity-Aware Distributed Inference  
**Milestone:** C4.3b — S2 State-Lineage Safety, adversarial EV0  
**Tracking:** #58 under #55 / umbrella #44  
**Base:** `b34877f400900c666db863d2cc4d4d8195a28746`  
**Status:** IN PROGRESS — initial corpus candidate

---

# 1. Research boundary

C4.3b extends the closed C4.3a State-lineage harness without changing C1, C2, C3, the baseline information contracts, or the C4.1 correctness record.

The canonical safety property remains:

```text
Consume(x, κ) => Compatible(x, κ)
```

Primary metrics remain:

```text
WBRR
WSCR
SSER
```

The experiment preserves the five distinct C4.3a stages:

```text
policy placement
!= worker execution rule
!= State consumption event
!= application result production / detection / recovery
!= authoritative terminal finalization
```

Every B0-B4 trial uses the same policy-neutral local-valid-replica consumption directive and the same result-bearing terminal-finalization machinery already established in C4.3a.

---

# 2. Adversarial manifest

The corpus schema is:

```text
cadi.c4.3b.state-lineage-adversarial.v1
```

It contains 16 canonically ordered cases across five pressure families.

## A — Branch ancestry

```text
S2A-DEEP-SIBLING
S2A-DEEP-ABANDONED
S2A-DEEP-ANCESTOR-CONTROL
```

The first two are exogenous WBRR opportunities. The third is a positive control.

## B — Producer authority

```text
S2B-THREE-GEN-SUPERSEDED
S2B-COMMITTED-PRODUCER-CONTROL
S2B-REQUEST-ORIGIN-CONTROL
```

The adversarial case creates State from A1 while A1 is CURRENT, then advances through A2 to committed A3. The physical A1 State remains VALID while its producing Attempt becomes SUPERSEDED.

The controls exercise valid State tied to the committed producer Attempt and valid request-origin State.

## C — Phase context

```text
S2C-PHASE-LATER-CONTROL
S2C-PHASE-SAME
S2C-PHASE-EARLIER
S2C-PHASE-NO-CONTEXT
```

These cases pressure the Paper-1 same-Attempt Phase ordering rule:

```text
consumer Phase ordinal > producer Phase ordinal
```

The later-Phase control is valid under exact C1 `ExecutionContext` semantics.

The same-Phase, earlier-Phase, and no-consumer-Phase cases are incompatible.

## D — Derived-State dependency

```text
S2D-DERIVED-VALID-CONTROL
S2D-DERIVED-INVALID-DEPENDENCY
S2D-DERIVED-SUPERSEDED-DEPENDENCY
S2D-DERIVED-MIXED-DEPENDENCY
```

The top-level candidate remains physically plausible and may remain logically `VALID`, while recursive dependency semantics determine compatibility.

The mixed case contains one valid ancestor dependency and one dependency whose producing Attempt later becomes SUPERSEDED.

## E — State validity

```text
S2E-TOP-LEVEL-INVALID
S2E-VALID-CONTROL
```

The adversarial case retains a physically `VALID` replica of a logically `INVALID` State. The control preserves ordinary valid ancestor reuse.

---

# 3. Independent exact-context oracle

C4.3b does not call either:

```text
ContinuityCore.is_ancestor(...)
ContinuityCore.state_compatible(...)
```

inside its independent compatibility oracle.

The oracle independently evaluates:

```text
Program/Session/Continuation/Request/Attempt linkage
CURRENT consumer Attempt authority
consumer Phase linkage/status when present
Continuation ancestry
StateValidity
producer Attempt authority
committed producer Request identity
same-Attempt producer/consumer Phase ordering
recursive derived_from dependency compatibility
```

The result is then compared with direct C1:

```text
core.state_compatible(candidate, exact_execution_context)
```

Any disagreement aborts the trial rather than becoming policy-visible evidence.

---

# 4. Frozen C3 Phase-context boundary

The closed C3 B4 semantic delegate does not carry `PhaseID` through its placement interface.

Therefore C4.3b explicitly separates:

```text
C1 exact-context compatibility
```

from:

```text
B4 effective compatibility through the frozen C3 placement surface
```

For:

```text
S2C-PHASE-LATER-CONTROL
```

expected behavior is:

```text
exact C1 context       compatible
frozen B4 placement    cannot prove same-Attempt Phase ordering
B4 action              recompute conservatively
correctness result     safe
```

This is recorded as a safe-conservatism diagnostic, not a correctness advantage and not an H2 violation.

C4.3b must not add PhaseID to C3 merely to remove this conservatism.

---

# 5. Baseline fairness

The C3 contracts remain unchanged:

```text
B0  request/load only
B1  candidate-key cache locality
B2  session affinity + B1
B3  exact StateID + precise State location, causally blind
B4  frozen Continuity placement view + read-only C1 delegation
```

Adversarial incompatible candidates are not fabricated unrelated objects. They remain:

```text
real C1 State identities
PREFIX / KV
physically resident at w1
replica status VALID
candidate-key plausible
```

where the scenario permits physical reuse.

The worker geometry remains the C4.3a geometry:

```text
w1  candidate-local, normalized load 1
w2  non-local, normalized load 0
```

Thus B1-B3 may rationally prefer the physically attractive candidate, while B4 may reject it only through the semantic information its frozen contract exposes.

---

# 6. Metric causality

## WBRR

Exogenous paired WBRR opportunities exist only for:

```text
S2A-DEEP-SIBLING
S2A-DEEP-ABANDONED
```

A violation requires an actual incompatible `StateConsumptionEvent`.

## WSCR

WSCR remains policy-derived. A denominator exists only when an actual State-consumption event occurs during a faulted operation.

## SSER

Application effects remain independent of compatibility and Gate metrics.

The ten faulted incompatible cases intentionally use:

```text
WRONG_UNDETECTED       4 cases
DETECT_AND_RECOMPUTE   3 cases
CORRECT_RESULT         3 cases
```

Therefore incompatible consumption can produce O4, O2, or O1.

---

# 7. Deterministic candidate expectation

If the closed policies behave as designed, the candidate corpus should produce:

```text
Policy  WBRR   WSCR    SSER
B0      0/2    0/0     0/10
B1      2/2    10/10   4/10
B2      2/2    10/10   4/10
B3      2/2    10/10   4/10
B4      0/2    0/0     0/10
```

For B1-B3, the ten faulted outcomes are expected to partition as:

```text
O1  3
O2  3
O4  4
```

For B0/B4, all ten faulted incompatible cases are expected to recover by recomputation and therefore classify O2.

This table is an implementation expectation, not yet a validated result. It must be replaced or confirmed from exact-head CI and bounded review before merge.

---

# 8. Positive controls

B4 must still consume compatible State for:

```text
S2A-DEEP-ANCESTOR-CONTROL
S2B-COMMITTED-PRODUCER-CONTROL
S2B-REQUEST-ORIGIN-CONTROL
S2D-DERIVED-VALID-CONTROL
S2E-VALID-CONTROL
```

The Phase-later control is separately expected to be safe-but-conservative because the frozen C3 interface lacks PhaseID.

Thus zero B4 Gate violations cannot be achieved by globally disabling State reuse.

---

# 9. Anti-false-zero obligation

The test suite injects an explicit incompatible B4 consumption event into a case that ordinary B4 placement rejects.

The harness must then increment both:

```text
WBRR
WSCR
```

and, under a `WRONG_UNDETECTED` application profile, reach O4 after authoritative terminal finalization.

This proves the metric path measures a real wrong-State event instead of suppressing or aborting it.

---

# 10. Exit criterion

C4.3b may merge only after:

```text
16-case manifest reproducible
all five pressure families covered
Phase-origin pressure covered
derived-State dependency pressure covered
independent oracle == exact C1 compatibility in every case
B4 WBRR == 0 across covered exogenous branch cases
B4 WSCR has no incompatible-consumption numerator
positive controls prove compatible reuse remains enabled
safe Phase-context conservatism explicitly recorded
Python 3.11 / 3.12 / 3.13 CI green on exact head
bounded exact-head review has no substantive correctness or measurement finding
```

C4.3c EV1 distributed CPU State-lineage validation follows this slice.
