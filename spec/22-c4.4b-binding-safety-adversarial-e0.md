# 22 — C4.4b Binding-Safety Adversarial EV0
## Deterministic S3 Adversarial Sequence Validation Record

**Project:** Continuity-Aware Distributed Inference  
**Milestone:** C4.4b — S3 Binding Safety, adversarial EV0  
**Tracking:** #65 under #62 / umbrella #44  
**Base:** `d7b39a1ada826dd0a30326793104d9445a5a22b3`  
**Initial validated candidate:** `d0ea459cf219b63f363cd773ab3562dc461591c1`  
**Initial validation run:** `33996050909` — Python 3.11 / 3.12 / 3.13 all successful  
**Status:** IN PROGRESS — adversarial result validated; documentation-synchronized exact-head CI and bounded review pending

---

# 1. Research boundary

C4.4b extends the closed C4.4a Binding-safety harness without changing C1, C2, C3, the baseline information contracts, or the C4.1 correctness record.

The canonical Binding safety rule remains:

```text
ownership-sensitive commit(y, e) => e == CurrentEpoch(y)
```

with safe migration requiring sufficient Evidence and one semantic transition of `(CurrentEpoch, ActiveBinding)`.

The experiment preserves the C4.4a stage separation:

```text
physical transfer/materialization observation
!= policy migration admission
!= authoritative C1 migration commit
!= stale/conflicting Binding presentation
!= semantic outcome
```

---

# 2. Baseline fairness

All B0–B4 retain the closed common C1 `commit_migration(...)` authority.

```text
B0  request/load only
B1  candidate-key cache locality
B2  session affinity + B1
B3  exact StateID + precise State location, Binding-blind
B4  BindingID + Binding epoch + reconciliation in the frozen Continuity view
```

Only B4 exposes the frozen `decide_migration(...)` admission surface. Its `WAIT`/`ALLOW_COMMIT` result is diagnostic evidence; authoritative correctness is determined by C1 state after the actual Binding presentation.

No weaker baseline bypasses the C1 base-epoch/status/evidence guard.

---

# 3. Pressure families

C4.4b covers five deterministic adversarial families:

```text
A  multi-epoch depth and reordered stale-owner presentation
B  concurrent candidate races and winner ordering
C  migration Evidence scope/sufficiency faults
D  partial materialization / failover before commit
E  repeated/replayed stale Binding presentations
```

Ambiguous Evidence is intentionally not used as an S3 comparator pressure because ambiguous-commit safety is primarily C4.5/S4. C4.4b uses only missing or mis-scoped `EXACT_OBSERVATION` Evidence to exercise D5.

---

# 4. Validated corpus

The corpus contains 16 deterministic cases:

```text
A-TWO-HOP-EPOCH1-LATE
A-TWO-HOP-EPOCH2-LATE
A-THREE-HOP-EPOCH1-LATE

B-THREE-CANDIDATES-LOWEST-WINS
B-HIGHER-CANDIDATE-WINS-FIRST
B-THREE-CANDIDATES-MIDDLE-WINS

C-MISSING-COMMIT-EVIDENCE
C-WRONG-BINDING-EVIDENCE-SCOPE
C-WRONG-EPOCH-EVIDENCE-SCOPE
C-VALID-EVIDENCE-CONTROL

D-DESTINATION-FAIL-BEFORE-COMMIT
D-PARTIAL-THEN-ALTERNATE-COMMIT

E-DUPLICATE-WINNER-COMMIT
E-REPEATED-STALE-LOSER
E-MULTIHOP-OLD-OWNER-REPLAY
E-SEQUENTIAL-NEXT-EPOCH-CONTROL
```

Fourteen cases are faulted. Two are positive migration controls.

Each faulted case declares exactly one exogenous paired SBDR EventID before policy execution. The executable regressions verify that ground truth and exogenous EventID signatures are identical across B0–B4.

---

# 5. Multi-epoch and concurrent pressure

## Family A

The first three cases commit two or three sequential migrations, then present a stale epoch-1 or epoch-2 Binding.

Validated authority examples:

```text
epoch 1 -> epoch 2 -> epoch 3
late epoch 1 or epoch 2 -> rejected
CurrentEpoch remains 3
```

and:

```text
epoch 1 -> epoch 2 -> epoch 3 -> epoch 4
late epoch 1 -> rejected
CurrentEpoch remains 4
```

## Family B

Several candidates are allocated from one base epoch before any commits:

```text
b2 epoch2, b3 epoch3, b4 epoch4 all base1
```

The validated corpus includes cases where:

```text
b2 wins
b3 wins before b2
b3 wins among b2/b3/b4
```

Only one Binding becomes authoritative. Later candidates whose `base_epoch` no longer equals `CurrentEpoch` are fenced even when they hold otherwise valid commit Evidence.

A higher allocated epoch winning first is not itself invalid under the closed model: D2 requires monotonicity, not contiguous epoch consumption. The tested safety requirement is single current authority and stale-base fencing after semantic commit.

---

# 6. Evidence and partial-failover pressure

Family C uses a single next-epoch candidate and varies only commit Evidence:

```text
no Evidence
EXACT_OBSERVATION with wrong Binding scope
EXACT_OBSERVATION with wrong epoch scope
valid EXACT_OBSERVATION control
```

The first three fail semantic commit and remain explicit WAIT/non-success. The valid Evidence control commits normally.

Family D models:

```text
destination failure before semantic commit
partial destination materialization followed by a different successful migration
```

Physical destination existence/failure is deterministic experiment evidence only; it does not mutate Binding authority.

The post-alternate-commit partially materialized candidate is subsequently fenced by the stale base epoch.

---

# 7. Replay pressure

Family E includes:

```text
duplicate winner commit after the winner is already ACTIVE
repeated stale loser presentation after an earlier rejection
repeated old-owner presentation after three epoch advances
valid sequential next-epoch migration control
```

The closed C1 migration API rejects re-presented committed/stale candidates rather than treating them as a second semantic commit. The validated property is that replay does not change authoritative Binding state.

The corpus distinguishes semantic Binding replay from transport-level duplicate identity. Transport cardinality/duplicate-event identity remains reserved for C4.6, where idempotence/ordering is the primary mechanism under test.

---

# 8. Oracle boundary

Every manifest declares before policy execution:

```text
expected final BindingID
expected final epoch
fault class
SBDR EventID
presentation BindingID
Evidence mode
reconciliation input
```

These expectations are independent of policy output.

Nominal setup commits are executed and recorded separately from the final adversarial presentation. Setup commits must satisfy C1 invariants; malformed setup fails the experiment rather than being attributed to the final SBDR denominator event.

After the final presentation, the classifier compares actual C1 authority against the manifest pair.

---

# 9. Metric contract

## SBDR

The 14 faulted cases provide 14 exogenous paired opportunities per policy.

A violation requires actual authoritative divergence from the manifest after the final presentation.

Rejected, WAITed, or failed stale operations are not SBDR violations.

## SSER

The same 14 faulted operations form the SSER denominator.

The validated faulted outcome partition under the common C1 authority is:

```text
O1  10
O2   0
O3   4
O4   0
```

The four O3 cases are:

```text
missing commit Evidence
wrong Binding Evidence scope
wrong epoch Evidence scope
destination failure before commit
```

---

# 10. Anti-false-zero regression

As in C4.4a, a private corruption hook is used only by regression tests after the real C1 presentation path.

Forcing the stale presented Binding to become current produces:

```text
SBDR > 0
O4
```

The regression first observes the real C1 stale presentation being rejected, then injects the corruption. This demonstrates that a real Binding divergence remains measurable and is not hidden by a pre-classification assertion or normal C1 rejection.

---

# 11. Validated adversarial result

The executable summary regression validates a semantic null comparator for authoritative Binding safety:

```text
Policy  SBDR    SSER
B0      0/14    0/14
B1      0/14    0/14
B2      0/14    0/14
B3      0/14    0/14
B4      0/14    0/14
```

Initial exact candidate `d0ea459cf219b63f363cd773ab3562dc461591c1` passed workflow `33996050909`:

```text
Python 3.11  486 passed in 18.88s
Python 3.12  486 passed in 20.80s
Python 3.13  486 passed in 20.25s
```

The only workflow warnings were GitHub Actions runtime deprecation notices for Node-based actions; they are not experiment failures or C4.4 evidence.

C4.4b therefore strengthens the covered deterministic evidence for Binding epoch/base-epoch/evidence semantics but still does **not** establish an independent B4 semantic-correctness advantage. Competent B0–B3 retain the same closed C1 migration commit authority and consequently reproduce the same authoritative Binding outcomes.

This semantic null comparator must remain visible in H6/S3 adjudication rather than being benchmark-selected away.

---

# 12. Exit criterion

C4.4b may merge only after:

```text
16-case deterministic corpus is reproducible
14 exogenous paired SBDR EventIDs are policy-invariant
all five pressure families are exercised
multi-hop stale owners remain fenced
concurrent candidate races produce one authority
missing/mis-scoped Evidence cannot commit
positive controls still migrate
replay/repeat pressure cannot change authority
anti-false-zero injection produces SBDR + O4
no C1/C2/C3 source or semantic changes
Python 3.11 / 3.12 / 3.13 CI green on documentation-synchronized exact head
bounded exact-head review finds no substantive correctness/measurement issue
```

C4.4b does not close H6/S3. C4.4c measured distributed CPU migration/failover evidence remains required.
