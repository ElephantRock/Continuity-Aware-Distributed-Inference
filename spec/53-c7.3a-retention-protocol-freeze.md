# C7.3a — Retention Protocol Final Freeze

## Status

Final pre-result retention-policy protocol freeze for issue #140 / parent #139, based on exact C7.2 closure main:

```text
47f1574c79f84c2e32e432a5da0f0522a6436937
```

No P2/P3 comparative result was inspected while establishing, repairing, reviewing, or freezing this protocol.

## Parent protocol binding

```text
C7.1 schema
cadi.c7.1.efficiency-protocol.v1

C7.1 fingerprint
706e0d5fff362a1eda8c906b957c914251c6e7949bac6be3c2c143ae21625474
```

## Frozen C7.3a identity

After the final bounded pre-result review, candidate head

```text
084e3b9e591165323d7fb1170e52f4ad6003438f
```

reproduced the canonical protocol twice byte-identically and established:

```text
C7.3a schema
cadi.c7.3a.retention-protocol.v1

C7.3a protocol fingerprint
f6165cb9248f5f4292846942e797e5ddcd8d004027ad503b2a7de35210021379

State-size-map fingerprint
36bacaad6462ca3e5eeb2688040e4dcf0d829849158c5b5ad9bad3c58afe173b
```

The protocol fingerprint and State-size-map fingerprint are independently asserted outside the protocol implementation in the dedicated test and workflow layers.

## Frozen State-size binding

The C7.3a P2/P3 reference State design point is:

```text
state_tokens       16          P-SRC4 experiment design
state_fixed_bytes  0           accepted C6.4f carried State-memory mechanic
bytes_per_token    524288      SIMULATED_SOURCE_MODEL_DERIVED_P_SRC2
state_bytes        8388608
```

The retention manifest fails closed unless its State-size-map fingerprint, State token count, and State byte count match this exact mapping. These bytes are source-model-derived evidence, not direct A100/H100 measurement.

## Frozen Tool-Return TTFT domain

Tool-Return TTFT remains:

```text
first generated-token completion
-
tool-return / resume-eligibility time
```

The modeled first-token completion includes queue/resource delay, required recompute-prefill, and the first carried C6.3 decode step only. Because the closed C6 runtime requires positive decode context and `input_tokens + output_tokens <= 4096`, the first generated token is evaluated only for:

```text
1 <= full_context_tokens <= 4095
```

Context 4095 is admitted; context 4096 is rejected. No out-of-domain affine decode extrapolation is permitted.

## Review corrections incorporated before freeze

The bounded review resolved all identified pre-result degrees of freedom before this identity was frozen:

1. terminal-but-valid State is not commonly released; LRU and fixed TTL do not receive lifecycle for retention ranking or release;
2. equal-time validity/lifecycle/policy-release/TTL/reuse/admission/capacity/accounting order is explicit;
3. fixed TTL is admission-anchored, does not refresh on reuse, expires before reuse at equality, and uses 5 seconds as the ordinary baseline with the predeclared sensitivity grid reported separately;
4. session pinning and B4 strict protected sets cannot silently overcommit capacity; infeasible points are labeled `CAPACITY_INFEASIBLE`;
5. useful/wasted residency is byte-second based and partitions classified resident byte-time;
6. the State design point is fixed at 16 tokens and the retention manifest binds its exact accepted C6.4f byte mapping;
7. Tool-Return TTFT is fail-closed to the accepted C6 first-token decode context domain `1..4095`.

Any scientific change to these rules after P2/P3 outcomes are observed requires a new retention-protocol version and preservation of this version and prior results.

## Establishment evidence

Exact candidate-head C7.3a workflow:

```text
run     34647039705
result  PASS
contract tests 46 PASS
protocol reproduction byte-identical
comparative_result_inspection NONE
```

Exact candidate-head deterministic-core:

```text
run     34647039665
result  PASS
Python 3.11 / 3.12 / 3.13 PASS
```

A subsequent exact-head replay after the independent fingerprint freeze is required before merge; that replay, not this establishment run alone, is the final merge evidence.

## Non-claims

This freeze does not establish a P2/P3 policy ranking, H5 support, direct hardware performance, physical cache residency from trace prefix fields, empirical prevalence of Continuity semantics, universal lifecycle-policy superiority, or Gate G2. It closes only the pre-result retention-policy protocol degree of freedom required before C7.3b mechanics implementation.
