# C7.3a — Retention Protocol Final Freeze

## Status

Pre-result retention-policy protocol freeze for issue #140 / parent #139, based on exact C7.2 closure main:

```text
47f1574c79f84c2e32e432a5da0f0522a6436937
```

No P2/P3 comparative result was inspected while establishing or reviewing this protocol.

## Parent protocol binding

```text
C7.1 schema
cadi.c7.1.efficiency-protocol.v1

C7.1 fingerprint
706e0d5fff362a1eda8c906b957c914251c6e7949bac6be3c2c143ae21625474
```

## Frozen C7.3a identity

The establishment run on reviewed candidate head `1220077e5c8bcdfe36c52cb031f66f3a608f2b16` reproduced the candidate twice byte-identically and established:

```text
C7.3a schema
cadi.c7.3a.retention-protocol.v1

C7.3a protocol fingerprint
ab3ade7ea5ebe98f8a1e359256ceb9cf07ad48acee431cea5f36dc7aca3e5622

State-size-map fingerprint
36bacaad6462ca3e5eeb2688040e4dcf0d829849158c5b5ad9bad3c58afe173b
```

These values are independently asserted in the test/workflow layer; the protocol implementation does not derive its expected fingerprint from those assertions.

## Frozen State-size binding

The C7.3a P2/P3 reference State design point is:

```text
state_tokens       16          P-SRC4 experiment design
state_fixed_bytes  0           accepted C6.4f carried State-memory mechanic
bytes_per_token    524288      SIMULATED_SOURCE_MODEL_DERIVED_P_SRC2
state_bytes        8388608
```

The retention manifest fails closed unless its State-size-map fingerprint, State token count, and State byte count match this frozen mapping. This is source-model-derived evidence, not direct A100/H100 measurement.

## Review corrections incorporated before freeze

The bounded review resolved the following pre-result degrees of freedom before this identity was frozen:

1. terminal-but-valid State is not commonly released; LRU/fixed TTL do not receive lifecycle for retention decisions;
2. equal-time validity/lifecycle/policy-release/TTL/reuse/admission/capacity/accounting order is explicit;
3. fixed TTL is admission-anchored, does not refresh on reuse, and expires before reuse at equality;
4. session pinning and B4 strict protected sets cannot silently overcommit capacity; infeasible points are labeled `CAPACITY_INFEASIBLE`;
5. Tool-Return TTFT uses queue delay plus required recompute-prefill plus the first carried C6.3 decode step and requires positive decode context;
6. the State design point is fixed at 16 tokens and the retention manifest binds its exact accepted C6.4f byte mapping.

Any post-result scientific change to these rules requires a new retention-protocol version and preservation of this version/results.

## Non-claims

This freeze does not establish a P2/P3 policy ranking, H5 support, direct hardware performance, physical cache residency from trace prefix fields, empirical prevalence of Continuity semantics, or Gate G2. It only closes the pre-result retention-policy protocol degree of freedom needed for C7.3b mechanics implementation.
